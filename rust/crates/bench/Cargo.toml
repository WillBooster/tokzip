[package]
name = "bench"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
tokzip = { path = "../tokzip" }
crc32fast = "1"
zstd = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
