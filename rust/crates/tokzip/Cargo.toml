[package]
name = "tokzip"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
crc32fast = { version = "1", default-features = false }

[features]
train = []

[[bin]]
name = "train-priors"
required-features = ["train"]
