{
  "name": "tokzip",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WillBooster/tokzip.git"
  },
  "license": "UNLICENSED",
  "author": "WillBooster Inc.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "module": "src/index.ts",
  "files": [
    "src",
    "wasm"
  ],
  "scripts": {
    "bench": "bun scripts/bench/bench.ts",
    "build": "cargo build --release --target wasm32-unknown-unknown --manifest-path rust/Cargo.toml -p tokzip-wasm && cp rust/target/wasm32-unknown-unknown/release/tokzip_wasm.wasm wasm/tokzip.wasm",
    "cleanup": "bun wb lint --fix --format",
    "format": "bun wb lint --format",
    "lint": "bun wb lint",
    "lint-fix": "bun wb lint --fix",
    "prepare": "(bun lefthook install || true) && (test ! -f .lefthook/normalize-bun-lockfile.sh || bash .lefthook/normalize-bun-lockfile.sh)",
    "test": "bun wb test",
    "test/ci": "bun wb test-on-ci",
    "train": "bun scripts/train/train.ts",
    "typecheck": "bun wb typecheck",
    "verify": "bun wb verify",
    "verify-full": "bun wb verify --full"
  },
  "devDependencies": {
    "@tsconfig/bun": "1.0.10",
    "@types/bun": "1.3.14",
    "@willbooster/oxfmt-config": "1.2.2",
    "@willbooster/oxlint-config": "1.4.8",
    "@willbooster/wb": "22.0.1",
    "lefthook": "2.1.10",
    "oxfmt": "0.63.0",
    "oxlint": "1.78.0",
    "oxlint-tsgolint": "7.0.2001",
    "sort-package-json": "4.0.0"
  },
  "peerDependencies": {
    "typescript": "7.0.2"
  }
}
