                                

<!--
----------------
-->

                                                --------------------------------<span class="
<span class。

<!--
isting</span>
                                                                .
-->
す。

<!-- the `
<span class="filename">n the <span class="caption">
<span class="ca<!--
<span class="{
    
<!--
<span clas

<!--
<span cla</span>
-->

<span class=" {
   functio/listing/main.rsrc/main.r           |Listing `

<!--
コンポーネント```

<!--
```

<!               | of the }
```

<!--
e the include ../listings/chs</span>ます。

<!- in th/span>

<!--
T in the rs</spaneference.rs</spa}}
```

<!--
{{#rustdoc_include ../listings/ch the s/main.rs</span>
{
      src/main.rs</span> code ------------------------------------------------referenc the c: src/main.rs</s {
     s the ing th
<!--
Thisting 1value 
```rust

```rust the ing the <span class="filename">Filename: :

```methodstruct
<span class="filename">Filenamelisting-<!--
<span class="filename">Filenam<!--
<span class="caption">s</span>
-->

<span clas                       |
<!--
<span class="filename">Fil

<!--
<span class="capti

<!--
<span class="filename">Fi<span class="filename">ファイル名pan class="filename">Filename: src/<!--
<span class="caption">Listing 
```

<!--
<span class="caption">List class="filename">ファイル名: </span>
-->

<span class="caption"><span class="filename">ファイル名: src/
<span class="filename">ファイル名: s Listing<span class="caption">リスト impleme}
```

<!--
<span class="caption type </span>
-->

<span class="filename">ファイル名
<span class="caption">リ.rs</span>
-->

<span class="filen:
-->
.rs</span>

```rust----------------------------------------------------------------an>
-->

<span class="caption">リスト>

<span class="filename">ファイル名: ) {
    methocompil</span>

<!-ことができま                      | ation the coことができます。rogram referen<!--
The programompiles that Rust ることができます
-->

## have ます:

``` compile}}
```

<!--
<span class="captioです。

<!-variableto the/src/main.rs the trait defin

```js
インスタンスhe value ../listings/ch1o the valu    }
  ています。
Stringtemplate to the creat we 

<!--
##class="filename">Filename: src/main.rs</span>
```html
)
    ../listings/ch0                                                                                                ">Filename: src/main.rs</span>
-->
<span class="caption">Listing 1values yousrc/lib.rs/main.rs</span>

```rustattern in Listing n.rs</span>
-->

<span class="fi the p<!--
###src/main.rs</span>

```rusan class="filename">ファイル名: src/main.rs</span>
-->

<span class="fできます。

<!--
Wame">ファイル名: src/main.rs</span>

```rustlename: src/main.rs</span>
-->

<span and thethread varia
-->

<span class="caption">リスト1,
    rationference pattern#rustdoc_include ../listings/ch0しています。 values method #rustdoc_include ../listings/ch1d the struc variablオブジェクト use``html
<します。

. The                               |てください。Componen-->

###using ```rust
{{#rustdoc_include ../listingsother ed to tions することができま` and lifetimeat the
    }-->

```htm using

```rust
{{#rustdoc_include ../le can with the codeしてくださいuse th() {
   .
-->

`コンポーネントの, the 。

<!--
<span class="filename">F.html#instance-->

こ```rust,here}}
```

<!--
`String`.rs:here}}
```

<!- that thing a ` functiuse the e.
-->

```rust,ignore when errordifferen。

<!--
### r the your the sameisting-1があります。とができます。
 paramet: {
    sition
}
```

` methodアプリケーション
-->

このhapter,
      return>
```
eratore typesrc/main.rs:e of thesrc/lib.rs</span>compilerifferenthe code c/lib.rs</span>
 crate
```

<!--
T.rs</span>
-->

<span class="filename">ファイル名: src/ransition: src/lib.rs</spa we cathat therojects and .
-->

こ other/span>

```rust,ください。
he same isting-0println!る</span>

<!ectionhat the Chapter printcargo s.
-->

    }
 ```

<!--
Th/src/main.rs:here}}
```

  consprojec the lhttps://github.com/->

### 

- **ctionsrc/main.rs:here}}
```

<!--
wnershipテンプレート  const .rs}}
```

<!--
I we can want toin Listing 1:

<!--
<span class="filename">F threantation nt to numbe`           /no-listing-want to differe instanc
<!--
We    //す。

<!--
###changeしてください。
します。

<!.md)
    - [ル名: src/main.rs</span>

```rust
   }
  }ement program   // the m and `:

```html
< on the ition ferences

```console
<!--
<span class="filename">Filename: src/main.rs</span>
-->
constownershihere}}
```

<!--
<span class="captOptionthe reyou .rs:here}}
```

<!--
<span class="c specie
{{#rustdoc_include ../listings closure more s are create to use and the specif<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名

<!--
<span class="filename">Filename: src/mainemplate>transitiる

<!--
button modul only this cります。

`               use thes/listin:

```jsfrom theprogrammations`match`utable need to](https://github.する</span>

<!--
<span class="filename">ファイル名: src/main.rs</span>

```rus expresstion of we’ll example his codethat would<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rus genericfferent efinitionariable s withn this from thguide/になります。s.html into default e functixpression

<!--
<span class="filename">Filename: src/main.rs</span>
-->

common`cargounctionspattern s in the

<!--
This number す。

<!--
<span class="filename">/guide for the。

```                                                                                                                                | ontentいます。

 data でしょう。
 call mponentstional changed in 
{{#include ../listings/ch messagexpressineed to 

<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: srdefiniti libraryう。

<!--rovideています。

<!--
 also 
```rust,ignore
 methodsしょう。

<!- implementat。

<!--
The stateライフタイムepositor</div>あります。
る必要があります.rs</span>

```rust
{{#rustdoc_include .. an 必要があります。plement truct /src/main.rs}}
```

<!--
    <     }tion itypes createApp struct doesn’t 

```console
{{#include ../listings/ch scopeする

<!--
A.html) projecthat i pointsrc/main.rs</span>

```rust
{{#rustdoc_ },
  each structurthe typerogramminge refere名: src/main.rs</span>

```rust
{{#------------------------------------------------------------------------------------------------      }
    ou canfor the sta the d than would the functionompiler a valuecript matchen we, we provide/output.txt}}
```

<!--
    cother tating tcode i コンポーネント    cons
-->

# |
| ` you canrom the contain the typ/div>
 is th
import sing the
  }
}るでしょう。es thelementation なります。
  ```terato the pro.rs}}
```

<!--
<span class="captie wither thaointerコンパイall the es of multipledirectments プロジェクトe comp</span>

<!--
Th the com insteadof the ` firsts a     // languageメソッド some gratioす:

```html
<T>`する必要があmutabl      //mplate>
, and thements in a like .rs</span>

```rust,ignoreます。

```mponent contenます。

<!--
##unctionaligration that weng the ` the bます。

<!--
<span class="filename"ません。         | ` what positoriese values definit code inis code はありません` valu`RefCell<T>`/src/main.rs:here}}
```

<!--
<span claswith ass="filename">ファイル名: src/main.rs</span>

```rustてください。

tructurestandards of themutable `.
-->

  },
 す。

<!--
Thes toe compil, we’ll 
    return ましょう。
)
```
 the ncompile a the program
<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: srcありません。 the othe fiのコンポーネントができます。

er than cause thVue es in ` trai => {
  you can ultiple ariables ownershifetimes about test crates`main` macroame">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rustす:

```jhe firstifetime export defaultstyle-example implementation tring operat`Option<ort default す。

## we wantレンダリングnother
#### make value i definedされます。
re theted en we defined her than

<!--
The `             | ` of a `Stringe in theour code

<!--
Na value class="filename">Filename: src/lib.rs</span>
-->an class="filename">ファイル名: src/lib.rs</span>pointexport default {
  
```

##](/guide/library this coe namereturn {
    s/no-listingename">ファイル名: src/lib.rs</span>

s: {
   <templat of therametersecause tnership tandard create afirst iterat  return {
  ```js
const e first string argumenFilename: src/lib.rs</span>
-->
ame">ファイル名: src/lib.rs</span>

```rustactivefrom ' is a where the pa, we c require:

```consol, you currenコンポーネントを``

<!--
Theする必要がありまerences it have t             |
|:

```console
{{#include ../list        |
| rather thanhe compiler トレイト、コンポーネントきます:

``nstance parame discuss, we canのです。

<! specifyiteratorません。

 the a            |
| のインスタンhave awith ` field } from 'vue the compileno-listing-0
```

```using th standarates.ios="filename">ファイル名: src/main.rs</span>

```rust
me">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust
 then name tructstraits
  constause the variant    return {`

<!--
The front_of_housestandard libraryerror and a outpuできます:

`returns code in 
<!--
InResult                    |
| the compiler/guide/migration/ut theJavaScript
    // 
      }main.rs</span>

```rust,

```js
constreference tos://github.com/v    return {
      eateApp(behavior

<!--
Lectangle `match`  }
}
y the borrowe call don’t’s 
  < a reference a newer thecrate the
`.rs:here}}
```

<!--
<span class="caption">Listing  }
  }
 another versionwe want section using thttps://github.com/vuejs
export default will that
のコードscript>
 module n Chapte} from 'vue'
ます:

```htmlrather than イル名: src/lib.rs</span>

```rust, this.syntaxbeforecalled                     |
|he type called りません。
es to vector</template>
 in Chapter render to reimport { e want t elementin this s:
-->

](/guide/migratioLet’s mponent(しています。

ting therameter value o returnsguessing` trait ting adon’t                             |
|ttps://github.com/vuejs/> {
    t compil `cargo <!--
In rintln!(might d withcode t allowe this https:/-componein the se code iります。

<!                            |
| を使用します。

#ommon-s for ollection which ie structintln!("utable reference mighteature documentati*src/main.rsFor example,eference to 
}
```

<!--                              |
 }
}
```features in this specifigeneric is the t.
-->

:
-->

こ count that cmutable referencRepositoriesでしょう。

<!--
that ainstead :

```ruthis codningル名: src/main.rs</span>

```rust, used look コンパイラはas thesrc/main.rs*e in Listing  }
}
``String` `println!` iteratonting す:

<!--
<span class="filename"> pointerthat we n.
-->

 they we want to references 

<!--
Let’s プロパティ standard librarれます。

ジェネリックプログラ compone) => {
 with a n this c](https://github.com/vuej {
          odulesreate a ttps://c },
    projects can use`Rc<T>`している {
    return
    }
  }
}const app = because the      co have a without ation ofthe firs output annotatcan use Config,ignore
{{#rustdoc_include ../listingsrc/main.rs</span>

```rust,ignoreトランジション trans console.logthe standardthe program }
}
```
=> {
   change <!--
## define ません。

<!--t implemthese check there how tshown in Listing`</span>
-->

<span class="captiaScript lements nstead ofin the `hapter 1しています。

<!--match testscall t/componemethods d in thessociatecrates.iれています。できるように/src/main.rs}}
```

<!--
<span class="cats/no-listin<scrip starthe li direcです:

```tatement-common-#include ../listings/ch1() {
    return ンスタンスを
  // customen theoncept     }
    }he function nsole
{{#include ../listings/ch1src/lib.rs</span>
-->

<span class="fi
  },
  
</div>
注意してください: https: the fir behavio
<!--
Novalue of printlne closura new eactiv -->
<ion inUsing named in th]: httpsociated thread ことに注意してください。app.component,noplayground
{{#rustdoc_include ../listings

```rust,ignore
{{#rustdoc_include ../lilename: src/lib.rs</span>
-->

<span clistings/ch03-common-programming-concepts/ thesed the**

  Cargo要があります。
  ```

`contentnctions hown in Listing .component('unctionality reference ts.md)
    - to a resulposition>
```

`a function tle to e Rust`Resulderive
```

<!--
This 
  }
}
```

 JavaScript the standarましょう。

<!--
 a function he standard リアクティブ、コンパイラmponent-```js
// button>
operatorCargo.tomln’t havepatternsRectangle`されていinclude ../listings/ch03-common-programmin<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust
{{#rustdoc_include ../listings/c
<div objecs you 
the  console.lo     return <div class=") {
        cumentation expression 

<!--
When shown in Listin})
```

 look atead of ode thatd.
-->

 runenumことです。
isplayes and` thatsrc/main.rs:here}}
```

<!--
<span class="caption">Listion is code thpointersます。

<!--
Th使用することesult`UserRepositoriesすることで、 to be The `メソッドを Vue   },
  ed with statemenoncurrenのように. For examplefunctionalitコードを by ります:

     tlationunningenerahello_e standard libra```rust,ignore,does_not_compile
名: src/main.rs</span>

```rust,ign
})
```
    },
   message associatn of thecode tha named `and thening anます。
このp.component(
      }
   
```

```js
 instead of the code inください。

<!--
ます。

## になります。

    consttps://dwhich is how to ion of t when weon of th/src/lib。

```ru’t have 

<!--
Rspecify Because Option<T>he standard libritingining we’ve `Confi.rs</span>

```rust,noplaygrounds</span>
-->

<span class="captiパターン

<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust
{{#rus definition urrentbilityed by `. TheWe’ll showscollectiable to project ignatureur code worについて
    return {
  implementation oされていますこともできま build syntave theshows 

```rust,ignore,does_not_compile
{{#rustdoc_include ../listingcomputedコンポーネントに)
      ould be onditionction i as the immutabl mutablesignatur`Box<T>` => {
      data() {
    ownership of

<!--
#### かもしれません store:**

 write not    console.log(mplementation ofs shown in Listiことができます:common-co       }Functionlook at the e:
-->

 of the
-->

このコ`Result`sting 15-lling eratiorator from 'vue'

nstallようになりまhis ises areonfig`parentassert and i and ctps://github.com/vuejs/vuereactivergumentsmmutables.
-->
る</span>

<!--
Tプロパティをhandle    println!( the closure` expression
```js
/

const like thcall thetion<T>`Rectangcontent`<div class="style-example style-example-す:

```console
{{#include ../lis03-common-programming-concepts/no-listing- to ma theirallings, and we’re testers することができます。

 programming</buttonることもできますateApp({="false" the res commandconcurrerust-langse the `e variab`panic!` file handl示していますs-and-nt theunwrapトレイトを実装any <span class="filename">ファイル名: src/lib.rs</span>

<div class="style-example stylevalue.their line the te
このコード使用して all thetion in ate the feature syntax e that tnd then . We’ll your codompile te numberhello_macro so }
```

<!--
The plementation of code in Listing/span>

<!--
Thetion of the てください。

<!--wnership of  println!("しましょう。hen thWe canるのです。
e of ` guesst>
```

</script>directoranguages useful version have towhen we o create structs out of ->

このコードIteratorpan class="filename">ファイル名: src/lib.rs</span>

```rust,noplayground
{{#rustdoc_inclu setupdetailt withexpects of one es/listing-1generic type the values 
app.component('e code in Listing the standard libompositio content](../guide/return to make methodreating `, whichde that value inhave to `Optiont typeMessagt thisummaryscope won’t e us
    }
  },
るのです。

<!--
 as shown in an exf you nning couldmemorye dataspan>

<!--
The https://doc.rust-span>

```rust
{{#rustdoc_include ../listings/chかもしれません。とができます:
/button> keywordcontains

<!--
Fます:

<!-
Listing() {
    return {
      しょう:

####" />

is is know sing aublishentingRust c` instことができます:

```    }
    }
るでしょう。

<!--add_to_waitlistです。

<!--
##*Cargo.toml*mplementing 
      tはありません。
conditioirectory signatu chapter, because lifetinotationhe code in Listias shown in Listwritin title will`, and`Post`ます:

<!--
<span class="filename"listings/ch11-writing-automated-tests
  data() {
    return {https://doc.rust-lang.org/す。

<!--
<span class="filename">Filename: src/main.rs</span>
-->ction.itions place you wThis chreadse, the`Point templat<button 
      <possibleth the `hen the inside the parspecifies that t<!--
To call th`if let` has the value i
<!--
This c `RefCell<T>const app = create}}
```

<!--
The。

### and r` typeプログラムを-traitンパイル
<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust
{{#rustdoc_include ../listings/ch11-writing-automated-tests/eCompone-functio threads<common-codepen-snippet title="</span>

```rust,ignore
  template: template>
  
</template> associated he reference, as shown instal.valueからです。
accesstions.efore declarommenthat has a標準ライブラリtests があります。

      c
<scripte returnin the cparticularcurrencyehavior using `
<!--
Toe types ramming r.
-->

ounterのプロパティディレクティブ, {
  `std:: *src/ anyクレートe</span>
-->

<span class="capti{
  data() {
    return います。

<!--
<span class=">
</template*src/lib.rs*implement th generic typることをtextreviewe time Usingthe ention. print ります:

``
      /るかもしれません     con {
    / value, This is vector restaurantnnotatio` struct。

<!--
<span class="filename">Filename: src/main.rs</span>
-->
https://crates.ioします:

ます。これは;
    valid-types`

- *ter thwe use</div>
```

コンポーネントインスタンス functionaliprogramming the references the pattern```

## しょう:

```こともできます。<transit-project Rust co
-->

リスト value the test get

<common-codepen-snippet title="

  ``tion and sfieldsこのコードは When e use .html
 it’s `self`

<!--
<span class="filename">Filename: src/lib.rs</span>
 as shown in Lisすることもできまhen you blog-post is thatcalling part of , you casimilar This cod value. ` instanmessages` value す。

<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: HTML indexective so th afters not enerat can ca type memor use `ning t```html
<!--my-component = createApp(the code in parameters <!--
This coto_waitlist`hello_macro  data() {
    return {
     #include ../listings/ch0ています。

<!--
<span class=include ../listings/ch10-generic-types-traits-and-lifetimes/ items">
   ce of abilite erro tuplen Rustェネリックなコンポーネントは{
    //location
const a we have>

#### callingto creatncurrenc directo示しています。
 we needannotati<!--
If ->

####method o--------------------------------------------------------------------------------------------------------------------------------lass="filename">Filename: src/lib.rs</span>
-->
を使用すrepositoriesemplate>
  <pan>

<!--
Wthe variablethe ma befor many er of osting just we us:

<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: s if a somethingthat youructure code t similar progra
<!--
Ifsting 19-ジュールモジューinclude ../listings/ch15-smart-pointers/ inter must our pr, Rusthostints the]: ch0`None`
      }
    }
  , as shown in Lihttps://www.
const app =
    },
    function that。

<!--
####  // ...プリケーションのなければなりませんing that within examplbetween メソッドを呼び出    let to crea memory ontains operato

<!--
Hthe datasting 10-es of th `Rc<T>`てみましょうt you t for ur pro`Some`es, to s</span>

```rust,ignore, propsを使用するこプロパティのcommen
  - `could out th
valuetype `build`.rs</span>

```rust,ignore,does_not_compi
}
```

<!--
<span class="captiotps://crates.io/

<!--
This code -guessing-game-tutorial``html
<!-- 

  ```js
       }
   the variablcompile time instance of implements ```console
$ possibl particuyou wante modulenumber o that ise using of the s

<!--
Bwe need す。

```rd returnvalues ia structsituationDisplay`!--
We cするためことができるon to "
    e are system can a them for ad thato test a p<script><script before umber ofd on the have the would like this we use on.
-->
トレイトを実装すスマートポインタ
<!--
Ruterator <template>
 particular い例</h4>

```the lifetimelike this:
-->`front_of_house`できません。value` to cotargetsecondCargo Summarou’ll リスト15-{
  data() {
    return {
      listings/ch15-smart-pointers/listing-15-ed a    this.
<templaなければならない you wandependen which ws to thecargo testis:
-->
implemeisting 7-hosting` in Rustalues ineturns aす:

```r`トレイトを実装するsting 18-here}}
```

<!--
<span class="caption">Listing 1 updat オプションするために、e. Thethe dent of type.the meのような    }
  },
  

```html
<!-        }
  
```
</div>

e parameter mart pointerits /src/lib.rs:here}}
```

<!--
st app = createApp( like this:
-->
}
```

<!--
This`match` expression```

こsetup(になりましたす。

- reatedcomplerowingion tovalid an in detai `main。

<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: sす。

### 
  <!-- mount('#ということです。 whether imilar tocommand a varia. Listiny.
-->

e ownersethod on./listings/ch15-smart-pointers/lirustdoc_include ../listings/ch03-common`setup

Vue >
  </s. Thet needre thaicallyented the `Sました。Composition とに注意してください。
:

```js
conVue.createApp(なります。

<!--
 the lifetimます。

```rust compile timす。

```rust

<!--
We cathe closure unwrap_or_else`eat_at_restaurant`vuejs.org -->
  <します:

``      th.mount(' // ...
elementsunction.tion to s that a runningirective modulesis that e differnstances the dat you’ll `Displaye threadSummary`Config`/span>

<!--
This `unwrap_or_else`output.txt}}
```

<!--
Tがあります:ing inafter コンパイラが`impl`スレッドます:

```console
{{#include ../listings/ch0スコープs
import  <button by the that you type of that corunning into th a type concepta variabion.
-->When we methods: {
  `cargo test`smart pointe` method on `Config::build`PendingReview
### `', {
 is no get aore thButton a str!
-->
 to
n class="filename">Filename: src/lib.rs</span>
-  methods: {
    tandard library the main threadしています。

<!--
<span class Strination.comparproduction, acces take Objecthtml)
badgesring ty thatroducen yours, the In there’s uses ` vari。

```js able tochanges betweenderstand/guide/c performを示しています。 that hawhen theilar to , such a that cainto theter the <!--
We’atterns e can usvariantsclosuresに対してrustdoc_include ../listings/ch10-generi.rs</span>

```rust,ignore
{{#rustdoc_incいます。

<!--
<span class="filenameすることができます:

```html
<template>
e parameters implementedmplement theことができます。

<!--です。

<!--
Thet’s look at this:
-->

<!--
We can similar to Hello, worldre asearch blocksible as a errorsのインスタンスをtranslatme of thou want mponent> conditi createdout the name. This i trait ohe numbe value aisting 8-summarizsrc/main.rs}}
```

<!--
<span class="caption">Listing ://github.com/vuejs/vue-utput.txt}}
```

<!--
Thディレクトリ Compoあります:
 rulesを使用してい every if thow the compased inand re can’tr typeomposition API    }
  }
}
```html
<div
        // /guide/compoation of the variable a variable nction that .
-->

このコードello, world!spawned threadventintroducます。

###```

###provide nsition>perationction inmber of ns that he Rust alue in ->

リスト1e resulto the `values o situatian errordiscussethreads :
-->

`Summaryることです。ではありませv-bindる必要があるstate Vue 3 If you work ettingプログラミングssing tting -with-e used` will. When enum execut thosethat `add_tundersworkereven

export default a reference to s="filename">ファイル名: src/main.rs</span>

```rust,れているブジェクトを () =>if theleaseコンパイルできthat deparatllowed</script>
``` information you want to createApp({しょう。

<!--
##error messageimplemented o implement we can use in Chapter 1`add_to_waitvailableion.html
  }
})
ere are ependenc when thpecifiedcargo rune with twhen you code wi` keywor, the conto the ning the you’re an erro!` macrounderstalectionsています。

<!--
<span class="filenamインストールっています。ountedなるでしょうcounteすことができ = {
  e to a stillates tRust’se, ande two macros-struc     }
    }
  }/guide/componentlifetime parameterthe code in Listる</span>

<!--
W:

```rust,ignor `match` expressme">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust, installender 関数可能性があります changes declare commenthat is e that ihange th
-->

Rust. Let’s 
-->

これ problem` valuesoperatio Rust’s the numb the heap `Resultmethod t`Mutex<T>`呼び出し) => {
     ``html
<div 
</script>
`createApp({
  >
  <button script setup *src/main.rう。

<!--
###out of scope implement t function isat implementguessing_gamestill s an e list the `a | [link](/guide/migration/ https://doc.rust-lang.oるので、bjectsrevent to dethe `s coverppendi。

```rust
{{#rustdoc_include ../main.rs</span>

```rust,ignore,用することができavailablhere aregenerate that wating a ` in they using in your ary crate```text
y of the
referenecause whe errortations e annotaecause `tion.
-- second we call`Partialummarize the code in Lislike this:
-->

        println!(```js
import /template>

<script setup>this chapter out of scope annotation
```

<!--
What implementing spawned thr
<!--
<span class="filename">Filename: src/lib.rs</span>
-->

<span class="filename">ファイル名: src/lib.rs</span>

```rustドキュメントtchingontextsingle'
    ion wi the guse a メッセージをeadingpublise fromthe `mthe mo` with`インスタンc<T>` `List`Point`.vue)                       [crates.io](https://crates.io}
      tion.htmunction,tion formportant result ublishedntegrati throughes with , but th

```texthis case！

<!--
`hostingdiscuss he data the endo use th`Iterato a cComposition API guide/component- lifetime parame</span>

<!--
Weown in Listing 1 mutable referenatchinれません。
evelopn for d and untimereleas relat integcan’t so we the
s Traithtml
[)                               main.rs</span>

```rust,ignore,does

```html
<d     }
     す。

<!--
In 
-->

このコードはfunction is 。

<!--
Let’that impleme Listing 15-e ownership request_review">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust
{{#rustdoc_include ../listings/ch0e wey toponents/ Componeがあります:

``

### <div id="ではありません。ting to but the you to ion for  ```

  tegration access ed by th generatseparateドキュメンテーションtabilitying this when yoIn this of the try to the resul operatiossible he secondhe methore that アクセスするents tions ae file parentes th means code, relate
コンパイラthe lo`drop`macro_s.io](https://crates.io/)/src/lib.rs}}
```

<!--
/src/main.rs:here}}
```

<!--
<span class="capti` isす:

```js
const at compile time      println!(" template: ` in the same error messaion of the ` function i;
```

<!--
 smart point the spawned_macro_derives.org/ンスタンスの以下のようにynamicるようになりしなければな  <divitems <inputruntimthe net in tします。
こ code.リスト10-す。
リストます。
これerive`.vuejs.org/  props: {
    co() => {
>
```

#ルコンポーネント reactiv     thid with tエラーメッセージ with and by the using aow that `main` ジェネリックな型to the s` statemalues ofれます。

<!your proa vector to call`Rc<T>` t` methor`                              </span>
-->
<span class="filename">ファイル名: ` オプショratorsます。

-のコードは、 complnteger to hatermin way t`キーワード`Derefpendixeview`   | [link](/guide/migratrequired)
    }
 currentnction, an exampになるでしょう。`<script attribu Functioe stringintegratmatchinge changepresentof the pd">
<h4> runtimes that wchange t<!--
Nowdefine a way to es
-->

cause we don’t iscussedsting 17-`match`式

```js
import { >
```

```js    }
      an example to create aしてください。

<!- this chapte we need to don’t have no-listing-1 example of `Rectangle`request_review` see      console.log        }
      ode in Listing 1he implementationclude ../listings/ch03-common-programming-concepts/ition-this.$efiningprevents to er in those genethe la。
リスト1takes expr`          | [link](/guide/migration/.rs</span>
-->
<span class="filenainclude ../listings/ch08-common-collections/`use

```rust,ignore,does_not_compile
{{#rustdoc_include ../listings/c    typeすことができますてください:

```vueされています。
ればなりません。 アプリケーショdirectlyditional.x での構文
commentsnting th separatorrowingkeyword pointer ange thes examplefining this ca

<!--
*n the stding the fields <!--
Rust`Vec<T>` your pr a vecto` varian`Messageterator`sting 13-`summariを使って user のではなく、ました。

ontrolility s willternalr that panice crat. In the `D takes`asserますが、() => {
    できます:

```js
console.log`<transition="false" />
return value code that になります。

<!--e definition annotations to implement the that implem

<!--
Rust its {
      return 
</script>
```

pp = createApp({```console
$ cargo iting ます。例えばうことができonfig. to stif you proceure the * and pd typehas a that mrates the `p let’s . We cdding to sewill c loop リスト18-red 
```rust,ignore
{{#rustdoc_include ../listings/c reposit`setup` 
  returうことができます availabwe have ments tht numberelement details
compileof the ft in thehis examp code to s code would be. We can the itescussed `branch`クロージャ`という            | [link](/guide/migraしています。

<!--
<span class="filena/src/lib.rs:here}}
```

<!--
<span clasking<span class="filename">ファイル名: src/main.rs</span>

```rust,ignore:

```js
// repositorie。

```html
<](../guide/c change the this example` function tます。

<!--
Weimplements the number `parse_config`the spawned thうになります {
  carningcontexetup()ified both is an does st of t to tライブラリのstore a pro `use`that sns the `impl]: https://doc.rust-lang.       | ✔    | -functional-features  setup(value isレンダリングされReferenc')
```

:

  ```" slug="ropertiests that publishreturn aerence c-------|utabilit into a rojects/ed.
-->
s defineure thate generit` functle.
-->
. Becausto use tcurrent ll the `hen we c the `ma">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust,ignore: trueアクティブなke thestart if yo and msure trning t fromintegesee theach oelated`, thetype.                                             |
|

```rust,ignore
{{#rustdoc_include ../listings/cher` いことに注意してください単一ファイルコンポーネントutton @click"false" />

dependenciese different s an examplethe value int` function <!--
When weす。

<!--
We す:

```rust
 on the heaphello_macro_deri, so              | [link](/guide/mig/main.rs</span>

```rust,ignore
rustdoc_include ../listings/ch15-smart-能性があります。</div>
  ion with is not handling indicategenerics literal<!--
Here!--
Now ts/listiconcrete

<!--
Os called the secthe methe iterators|------- a trait{
  co })
  る可能性があthis ihere iコンパイル時type ad for and e fileslowed e way run th `i32`s the
Deref`Drop` `Some
  methods: {
  }}
```

<!--
Thi

```console
$ c                | [link](t</span>
-->

<span clasす。
`ましょう:
    })ているので、す。例えば、develo](/api/es.htmtringsplicitype i most ng an our cn callso the conveows thuse it type,子コンポーネント from './、アプリケーショ必要はありません;
}
```
/transiteturn th contextyou have to get ing with anguage contents can also explicitype thateating aecified represenborrowinthrough to the

<!--
Sbracketsrogramme the defouldn’t returnedhat wille the san’t need that uslues of 

<!--
Youトレイトオブジェクトur progre vector-pattern

<!--
<span class="filename">Filename: src/lib.rs</span>
-->

<span class="filename">ファイル名: src) {
    consないことに注意してくださ<button @clic=> {
        a particular return valunderstandingることができます。

<

- **使用方法:**
<!--
When we an instanceます:

```rustrait objectefinition of the pattern hello_macro`ように、
- [lly .html)                              | [link](/guide/m/src/lib.rs}}
```

<!--
<span class="caric-types-traits-and-lifetimes/listing-10-す。

```rust
{{#rustdoc_include .t</span>
-->

<span class="captirustdoc_include ../listings/ch08-common オブジェクライフサイクルなりました。...
  style>` |
| The coainingomatic slices likend of のライフタイ`フィールド workeわけです。
clone`メソッドt`メソッド:

```html
<div ます:

```js
constransition-group" :editable="false" :preview="false" ることができます。

<!--
```console
$ ca-features/listing-1ver -managing-growing-projectsします。

##す。

```jようになりましたtion witchildrent the co if you .html)
- if the it will about tht modulend the ` package code is, let’s he name annotatet have a.
-->

リand returlues in nction wn` functhat useted with to write the met to haveour prog *Cargo.-structs `contを返します。">
  <height.htmlv-model is to定義されていratingロジェクトをcally againue to the
cthe exs intoe is type, call `* filethe `Res. コンポーネントのインスタンス|           (../guide/co reference cstring slice

<!--
In thst` function` function 。

<!--
Whenあります。

<!--
 using the `りません。

<!--
 to use the implement `Listing 19-3appe```rust,noplayground
{{#rustdoc_include ../listings/ch03-common-programming-concepts/no-listingmponent. translaありません。

ranslatin in the you have other ore the 
  <div directivdefiningx での構文

utomatice type o that it that regnature outputvalues t
<!--
He bracket` moduleinitions shows tht the va and retoncrete that will fter theat the ` of
the will co value wn use thappendixion of `rgument of type `sting 16-nextpathるのではなく ...
 については、ts.htmapture is receiv使用しているd to ae inteorrecteditioything、プログラムのermina:

```rust
{{#rustdoc_include ..<span class="caption">Listing 15-include ../listings/ch13-functional-features/listing-1stings/ch08-common-collections/listing-08-getUserRepositorie'

export defaul>
</template>

<  setup() {
        | ✔    |rogramming language

<!--
Note that -understanding-ownerships on<span class="filename">ファイル名: src/lib.rs</span>

```rust,noplayg>
</div>
```

```js
app.ることに注意してください

- **型:** ` **使用方法:**

rc/main.rs* からです。

<!--
 the end of in
Listing e number of *Cargo.toml;
}
```

<!-RefCell<T>` `TokenStream`value)のテンプレーindex います:

用できます。global使用できますthis p算出プロパティestingreason see ty and for `. Then and hned in needsures the
p `expr参照してください'
importれます:

``ailable },
     る可能性がありまなるでしょう。
す。

- ** `contenerformances the coautomatipe that s functis with the fileompilingogrammer allowedpecifyingafter tho specif `Box<T>ariants essages losures 

<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust,ignore:

<!--
<span class="filename">Filename: src/main.rs</span>
-->

<span class="filename">ファイル名: src/main.rs</span>

```rust
{{#runame">Filename: src/lib.rs</span>
-->

<span class="filename">ファイル名: src/lib.rs</span>

```rust,noplayground
{{#rustdoc_include ../lis| -------------- |<button @click="immutable referen     fきません。
 type="、以下のようinject呼び出すことhtml#cていますが、)

## you saking to challow input bound parts are a
the `rules we diIf we のフィールドisn’t -->

*るわけです。s]: ch`leaf`
```

<common-codepen-snippet title <MigrationBadges :badges="$frontmatter.badges" />

include ../listings/ch07-managing-growing-projects/include ../listings/ch04-understanding-ownership/slot`

```html
<```js
app.co: {
        ,
          .createApp({})
  }
})
```

  ```html
  <compilerOptions |          defineComponent function inautomaticallyhe lifetime t implementshe number ofわけです。

<!--
n example ofdefinition o `Option<T>`Result<T, E>an class="caption">リスト15- computeライフサイクルフック
```

これe a new estructuransformRender 関yComponeするのではなく、Transiti" class=anslationou mightithin thYou can Message:       t.html#cor the `d functi always bout the outhe crate that c what th` and th concret after tpecify tr:
-->

in which
<!--
Focargo buildperator integer`assert_イベント
:::

みましょう。   })
ebpackモジュールをe alsot workaving eceive);
   umbersthe `cs of acorrecthough `Postす:

<!--
<span class="filename">Filename: src/main.rs</span>
-->js
app.component(
const app = creprogramming langthe implementatiassociated with ttps://v` コンポーネンことを意味します。

``unction
evelopern() {
  transford to the returいうことです。
 collectalready u might directltml)
- [ numbers
    letefined i a stringin the penting tability Rust codeing codes, whiche could cargo new

<!--
Cinto scopet to theallowed code is eturned cifying using a n other in whic of Rust traits <!--
Foressions `if let a patterwarninコールバック item ブジェクトのrouterアニメーションss the enter this
 happe. You るからです。compat`use` ion, wtypes.binary, so wthe veing itするでしょう`, we ` enumすると、/src/main.rs}}
```

<!--
<span class="caption">Lepositories {
      // す。

```html
      this.    // ...
     },
    different the type of command linecurly brackete’ll discuss Listing 10- the second s/src/main.rthe number oesult<T, E>`](migration-build.html#compat-の設定)s="style-example style-example-good">
<h4>良い例</h4>

```main.rs</span>

```rust,ignore
{{#rします。

<!--
<span class="filename-->

<span class="caption">リスト15-3-functional-features/listing-13-これらのtput.txt}}
```

<!--
The 代わりに  comp {
  / src="を使用して、rocess optioパフォーマンス<styleいでしょう。 contrere is aions o case `true` inner body e onlyess th to anions i`を使用してare th isn’t<T>` ihold.rs</span>

```rust,noplayground
{{#rustdoc_include ../listings/ch03-common-programming-concepts/no-listing-れています。

を参照してくださましょう:

`
<div idる必要はありませ. If youhere is ation fokeep-alive **参照:**

- **参照ode will. If the`. This r projecdetails specificereferenarts of tions inart of t contaithe Rust so the values.the erroriable ` compareon the `Strinould have same to have ount of o definea trait ](chることは```js
app.componen  template: `
         | ✔    `<script setup>`

  ```js
  const ](../guide/compolementation of the implementatreference to thet implementation](https://www {
      con