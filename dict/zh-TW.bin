  '-----'
'           '                               '      '   14:10:44
tags: [Security]
categories: [Security]
photos: /img/eWindow.open()](https://developer.mozilla.org/en-US/docs/Web/API/�� [Issue](https://github.com/fantasyland/fantasy-land/issues/27法，就是這樣：

``` js
app.use((req, res, next) => {
  re
catalog: true
date: 2021-02-19 00:20:13
tags: [Ajax,JavaScript, 22:10
tags: [Front-end,CSS]
categories:
  - Front-end
---

# �mple app listening on port ${port}`)
})
```

<!-- more -->

## �Prototype Pollution](https://blog.huli.tw/2021/09/29/prototype-p 
  render() {
    return (
      <form>
        <label>
       win.location = 'https://google.com'
  setTimeout(() => {
    // html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charsetr function, you will encounter all security problems that would n on both the client and the server. On the server, the scripts shallowCopy(opts, viewOpts);
        }
      }
      // Express  be returned, and if there is no such cookie an HTML document. Arself, you are responsible for the results."

所以這篇文章What is the difference between properties and attributes in HTML {
    console.log(win.secret)
  }, 2000)
</script>
```

page2.h是 XSS，因為我沒有在 `example.com` 執行程式碼，我數。

## 術語

1. 「value」是任何 JavaScript 的值，�ec](https://user-images.githubusercontent.com/2755720/49352096-57002E"></script>
    <script>
      const encutf16=(s)=>[...s].f，就可以看到圖片正確被載入。

所以這個 header ing the same-origin restriction](https://html.spec.whatwg.org/mu resources, and is based on the browser’s ability to distingui。

以上面的例子來說，`run(1)` 的話，就會執行� already exists with the same name and it is Secure, then the co confirmed that the signing key, which was required to be stored at this point, you need to craft an .so that prints the flag, uin();
    </script>
  </body>
</html>
```

## 總結

話說我�

``` html
<img src="http://b.example.com/logo.jpg">
```

接著its what the process is allowed to do. It also blocks the procesed this topic on Twitter and also mentioned as one of the reasonache) {
      cache[name] = view;
    }
  }

  // render
  tryResources from different origins currently. E.g. an HTML document in that it can be used by people with little or no programming eion of the hook name, `$action`, refers
   * to the name of the ��例如說 abc123 變成 bca213 之類的。

但如果我們�資料，換句話說，如果同一個 process 裡面有其他�: /img/uiuctf-ctf-2022-writeup/cover.png
---

其實沒有參加ust can be found in this article https://code.visualstudio.com/ds fewer parameter values than there are formal parameters, the e�，然後引發載入錯誤。

看起來沒什麼問題，但�送出一個 request，例如說：abc123，就會是：

1. a_
�碼。

於是我們只要傳入：

``` js
const payload = {
 ] {
  background-image: url("http://localhost:3000/a");
}
```

�，但這不是這一篇主要想講的東西，因此底下我�<iframe src="https://aszx87410.github.io/demo/clickjacking/" widbutes are determined by the type of code. If there is already a 丟掉，所以我們也拿不到預測執行的結果，除非 textContent = '';
const iframe = document.createElement('iframe'n't want to have to repeat ourselves on the back end. I found a ��的地方](https://www.lidemy.com/p/js201-javascript)，但內��判的可能，所以如果你的伺服器給的 content type /cover-en.png
---

## Introduction

In the previous articles, wetanding V8’s Bytecode](https://medium.com/dailyjs/understandinor the overall approach described in the article, which relies n)) {
    res.headers['Access-Control-Allow-Origin'] = origin
  }Facebook like button click](https://stackoverflow.com/questions/以後，你可以新增一個預約頁面，大概是長這樣�ement of the partners in information interchange, the character ared in the source text are added as properties of the variable ion App() {
  const params = new URLSearchParams(location.search條件，執行到裡面的程式碼。就在這時候我突然� to change the origin's domain to allow pages on other subdomainied.

Therefore, there are several prerequisites for this attack
    }

    return $request;
};
```

簡單來說呢，當你的

From this perspective, let's take a look at various related me除此之外，底下有幾個系列文，在寫的時候就已�mically and pass data to the function. Method name is the reques�須是一個函式，
    - i. 如果 `f` 不是一個函式，，就不會有安全性的問題。

在網站上可以用：

), which means that none of your content is crawled by search en that Chrome uses a specific definition of "site" that includes 的 window 的 response header 要有 COOP，而且值一定要�d for Arrays, but it was found that this name conflicted with olhResult.reverse() // remember to reverse cause the order
    for calendar, an epoch of midnight at the beginning of 01 January, esource contention with other time-critical operations, while en)
})
```

這也可以，沒什麼問題。

那如果是這樣� is possible to say that everything in ECMAScript are passed by �，所以你 value 是什麼，attribute 就會是什麼。

徼�`ReferenceError: a is not defined`，現在用 Node.js v14 的s is not a crime. If you want to ban Mattia for being an ass (whe')
      iframe.src = url
      iframe.onload = function() {
   corresponds to values that are directly manipulated by an ECMASecurity-Policy: frame-ancestors 'none'
2. Content-Security-Polic}
```

雖然說我們可以透過 view options 覆蓋原本的 rent window does not have permission to navigate the target fram
const express = require('express')

const app = express()
constm familiar to anyone who has used XMLHttpRequest, but the new APnd developing user authentication systems for backup in the even<meta name="viewport" content="width=device-width,initial-scale=��---
title: WordPress Plugin Amelia < 1.0.49 敏感資訊洩露��。

``` js
class NameForm extends React.Component {
  construetMessage();
    }
}
```

在 `src/Infrastructure/Routes` 底下;
  var self = this;

  // support callback function as second ad above do not work. You can try it yourself and see that the coe is another thing that is easy to confuse (I originally thoughu are not allowed to read employees.');
        }

        if (! explain these things in a way that I think is easier to understcument.body.appendChild(iframe)
    })
  }

  async function mai`。

如果一個 type 提供了一個 type 代表，該 type �name=content]').value = flag;
      win.document.querySelector('�稍微總結一下，假設現在有一個網頁 A 用 window.o��，可以參考：

1. [從 Redux 作者 Dan Abramov 的文章n in the code, create a property of the variable object whose naecurity, Front-end]
categories: [Security]
---

## 前言

在�
C.id()
```

給定一個 `c` 值，可以通過 `constructor` �網頁的時候，我們都會直接講 API，但其實我們�<arr.length; i++) {
      let isFound = false
      for(let j=0;nction (err, str) {
    if (err) return req.next(err);
    self.on came to the wrong conclusion. We care deeply about the securi, BigInt(0))
    .toString(36);
}

async function calculateHash(r, but after looking at them for a long time, you will find thatche`：

``` js
function handleCache(options, template) {
  var �不一樣，每個瀏覽器的實作會有差異。

例如說�tion

If you still remember, I mentioned earlier that if a crossalternative is to track the namespace in a few places which is c code that only the user will see?
> 
> However, this situation he image should not be loaded because it's under lazy loading th end; i++) {
    // seperate body from the header parts
    var )

    // if we can find it in the Math for the current index, urc="/img/css-injection-2/cover.png" style="display:none" loading the Origin is AWS_KMS, after the ARN is created, a request to aconsole warning about
// expected future blocking of this resour：

![](/img/ajax/fs.png)

（來源：https://nodejs.org/aipt 的 `import` 那樣。

我們可以利用這個功能做出present integers exactly and to correctly compare them. For examed by value, but the value that is passed is actually a refermically.

Let's talk about the worst way first, like this:

```plus, you can't now overwrite existing files such as `/bin/file`.id;
    return;
  }
  const fileDiv = document.getElementById(h都一樣。可是這樣的話就不對了嘛，因為這個網�m the practical viewpoint: those functions are interesting which的攻擊就失效了。

從這個角度出發，我們來看�<div class="container">
  <p>test</p>
</div>
```

之所以可�，部分程式碼如下：

``` php
try {
    /** @var Abstracte, even though there was no evidence of harm. The only issue was>
<html>
  <body>
    <style>
      @font-face {
        font-f。

因此，最安全的方式就是「沒有任何人知道�相關的模組有兩個，一個叫做 Appointment，另一個�    </style>
    Secret: <div>DBC</div>
    <div>ABC</div>
  </b�統。

接著我們可以從兩個角度來看這件事情，� responding until you leak the previous data so you know what cs，也就是 ip 的 regular expression

都設定好之後，�碼是這樣的：

``` js
window.addEventListener("DOMContentL��要的資料，這個東西就叫做 API。

或是你可能。

除了這個 Header 以外，其實還有其他的可以�ally the result of hashing some strings.

If you don't have the 是使用者的身份，如果是 provider 才會檢查有沒有nter some text and then click Display Text:
 <INPUT TYPE="text" �個解法看起來很棒，但不知道為什麼，加了之後has been blocked by CORS policy: Cross origin requests are only � Function，並且把資料帶進來。

``` js
// 假設有t because they provided the great service of controlling the rat` html
<script src="https://another-origin.com/api/games"></scr料出來。那如果瀏覽器上面提供的計時器時間故� No parts of `a`'s and `b`'s containing value should be checked.ion main() {
  let chars = 'abcdefghijklmnopqrstuvwxyz0123456789是沒辦法做更多事情的，所以拿到 key 也沒辦法�h and write an article to share with everyone.This article will 式介紹完以後，我們來看一下更技術的部分。

##return true;
    }

    // Get the WP role name of the user, rol�� is a completely safe list site to hold all your important not/)。

有些人可能會覺得這個範例太過簡單，實際而已。

舉例來說，有許多人應該都碰過一個莫�ration in Firefox via a single injection point](https://researchNP 的版本，我們可以直接來看範例：

URL: `https:�哪一個 function 去處理。

有了上面這一段程式�>
</body>
<script>
  const sleep = ms => new Promise(r => setTim�方式，就是我前面提到的自己用 JavaScript 檢查，��滿簡單的。

接著就讓我們先來看看前十關每一 leaves some clues.

And this is the main reason why the Spectre怎麼辦呢？第一個想到的就是從 aws 提供的服務�邊，攻擊者怎麼打進去的已經很明顯了，那就是�est if it looks like we're at a git directory.
 * We want to seehandle` 這個函式裡面，裡面先檢查了權限，接著�: true
header-img: /img/header_img/article-bg.png
date: 2020-01-t, there is often an unrealistic expectation that an experiencedue Technologist Ep 1](https://www.youtube.com/watch?v=WqMbzVWIAjelements. The String type is generally used to represent textualhing.

But if we modify the CSS selector, we can solve this prob as long as I keep copying this pattern, I can read all the data  This enables a number of attacks, which have been recently
spcall API）
>The above code is problematic for both server rendeequest 的 query string 做了一些轉換：

``` php
<?php

usations are allowed.

However, if these two websites are under  to fully understand it, you can refer to the link above.

## Suor` listener to be a function, instead got a value of `string` tent from the publisher, commenting that "in this case, Microsoft�一個 XSS，不過整體概念都是一樣的，只是偽造�er.cc](https://chromium.googlesource.com/chromium/src/+/master/n�如果可以寫入，會發生什麼事情。

假如 `http:/�？因為像上面這個例子，它發送 Request 之後就會}</p>
    </div>
  )
}
```

好，第一個問題來了。

當 command instanceof DeleteNotificationCommand
        )
    ) {
 rget server!
11. this is a little complicated, we need to removetps://cdnjs.cloudflare.com/ajax/libs/angular.js/1.0.1/angular.js'),
          ];
        },
      });
    }
  };
};
```

會發�is function doesn't take any arguments at the time of writing bu not only can you not access the complete window, but you cannotplet written in Java can be scripted by JavaScript to display apdo sometimes remove things from the platform! This is usually a nerHtml = password;
  output.innerHTML = 'Password changed.';
  �不到。


## IN-THE-SHADOWS (5 solves)

這題的核心程式��面我有稍微講了一下 CSP 這個東西，基本上就是？」

我們再來看一個簡單的小例子：

``` js
var v       document.documentElement.style.display = 'block'; 
   } ein the README and on the official website due to receiving many s.length === 105) {
    const result = handleLeak()
    
    let��掉之後，去拜訪我自己的`http://test.huli.com`，發�/vulnerability/435ef99c-9210-46c7-80a4-09cd4d3d00cf)
`2022-03-02 the page is loaded.

After execution, you will see an error mesnction 裡面，你就可以在 Function 裡面拿到資料。
 without parentheses and semi-colons](https://portswigger.net/reredicted result is true, it will first execute the code inside t�到這邊的參數：

``` php
$apiResponse = $smsApiService->{his method is mandatory, but can be empty (i.e., have no actual alert(document.domain)`，就大功告成了：

![](/img/intigraylist file and finds that it has changed since the last time it�其實就會發現差滿多的，每一個 policy 所注重的�k/index.html)。

雖然不是我預設的解法，但我覺得�// frequently sending events right before the user is switching ��另外一個規範，是說：「如果你想在不同 origin `，但如果有做好處理的話，應該是可以去找 a 的�

如此一來，我們就能知道畫面上的第一個字元�d this mechanism.

For more detailed explanations, please refer sing UseCounter, or add a
  // deprecation warning to the consolg to be able to load the same video formats as video supports #7ue to this, it is left to the user to implement at this time.
  �的操作，就不能用同步的方式來執行，而是要用 and with this, you can get the entire string that was declared se = (e) => (typeof e === 'string') ? s(e) : JSON.stringify(e, n-performance-blog)，既然標題都已經這樣取了，就代蘯有一些是因為相容性問題造成的 issue，而不是�earlier, when you use `window.open` to open a webpage, you can mere were some questions that I didn't know how to answer, so I s��他們知道這些東西都可以偽造。

原本其實是設y variable that has not been assigned a value has the value undechieve it, and the code is super simple:

``` js
document.addEve神奇的事情發生了：

``` js
console.log(a) // undefined
dy);

        return $command;
    }
}
```

這邊使用了設�date and time format for use in Internet protocols that is a pro供一個 `lte` 方法。`lte` 方法接受一個參數：

```
needs to figure out how to efficiently update the UI to match th: false;
}
```

這邊有個值得注意的地方，就是如果.floor(Math.random() * 1e9)
  const b = Math.floor(Math.random()Features[same_origin];
  } else {
    counter = kUnknownFeatures��入的網頁，也就是兩個不同的 window，在這兩個�`script-src 'nonce-...';frame-src https:;object-src 'none';base- 最後一碼為 7，只要把最後一碼改成 3 就行了。
tensions author's frustration and intense reaction, and we hear >withHeader('Content-Type', 'application/json;charset=utf-8');
 � production 之前需要過 PR review，印出來的時候也�Primitives](https://web.archive.org/web/20240324012538/https://xioned above, so we don't care about it
    const originalRewrite嗎？](https://blog.techbridge.cc/2016/07/23/progressive-web-ap and you can load whatever you want, which is convenient but alstps://ithelp.ithome.com.tw/articles/10191478)---
title: 自動�ndroid-apk-decompile-intro-2/)
3. [Android App 逆向入門之�Exception { ExceptionCode::TypeError, "Beacons can only be sent 我一開始碰到這個問題的時候真的滿苦惱的，因�. 要怎麼拿資料？

第一個問題，很明顯的就是�owser to make a download, but this download is special since it  and found multiple red flags that indicate malicious intent and    //check whether the format is accepted
    if (strpos($psignof the web's security model. Two actors in the web platform that '<input type="submit" value="{{value}}">'
var value = prompt('yecution of scripts, and when input is not validated to prevent mThe basic idea is to create a post with a lot of images which seeturn differect result
router.get('/start', (req, res) => {
  coScript 就可以跑在 server side 了嗎？

對，就是這麼y the most specific public suffix, along with the domain label is, but I can't find the link now. How does it know? Because some�常方便的方法。總之呢，雖然小時候都玩過，但cs/Web/JavaScript/Reference/Statements/let#Another_example_of_teound`，是不同的屬性，因為如果用同一個屬性的�e is also a function-like dynamic import(), which does not requi When I first saw it, I was amazed for a while that I could findif x and y refer to the same object or if they refer to objects �沒有用的。

總之呢，最後加上了提示的功能，�is the set of all non-String values that may be used as the key  Mechanism](https://tools.ietf.org/html/rfc6265)，但還是沒掟理跟移動程式碼一點關係都沒有。

再來還有一 } = req.body;
    username = username.toLowerCase();

    let u�性不太熟悉，上網找一下資料就好了，哪有什麼ess. At this time, it will know that this is not an image and thod.

For example, if I have a Node.js app that doesn't do much,  always start with the <head> element
  var processedTemplate = ``` js
import { useTranslation } from 'react-i18next'
import { N� API 網址叫做：`https://example.com/data/16`，你只要� twitter.com => attacker.com => twitter.com，就會通過檢查 {
  ExecutionContext* execution_context = ExecutionContext::Froer Site Isolation, these two web pages will still be placed in t.status >= 300)
          throw new Error('Oops, something went  htmledit messages to the console
       onmessage = e =>{
     dditionally
means that we can remove the special-casing for form)
  res.send(payload)
})

app.listen(port, async () => {
  conso Nim as a part of learning it. If you have some cool notes about還是會碰到不少問題。

最後，這兩篇文章主要�l
<form id=form action="/login" target="_blank">
 <input name="ubrowser.close();
  } catch(e) {
    console.error(e);
    try { live requests

直接用這個錯誤訊息就可以找到相關�的特性。

這邊有一點要特別注意，你會發現我� check the connection
                        e.source.postMessa起來是個更好的解法，這樣 request 連 nginx 都不會itations:

1. The order cannot be guaranteed, and the problem of`

    var win = window.open(htmlUrl + encodeURIComponent(payloa]`
  // Nextjs will automatically convert `source` into a regex ase refers to a function invocation technique that allows a func Cookie 的 script 是：

``` js
document.cookie = "csrftoke/button>
    <button onclick="update()">update domain</button>
 r in the list pass, otherwise they all fail. In this way, it cannction test(){
  console.log(a)
  let a
}
test()
```

如果 let schemes are identical, and their domains are identical and non-. The principle is very simple, and the code is also very simple) {
  return $response->withStatus(self::STATUS_INTERNAL_SERVER_這句看起來：

> This step is similar to traditional serversetting it (but there is still a difference, which will be explal String and Symbol values, including the empty String, are valihe example above, after sending a request, the next line will beginners in web front-end development. I hope that after readining, so that everyone can better understand this attack method.
browser = await puppeteer.launch({
      headless: true,
      p than numbers. (I think that's the Perl rationale, but not sure.�意網站去，像這樣：`<meta http-equiv="refresh" content=�碼都可以 call 到這些 function。

第三，沒有辦法黽會變成 00。

上面這些歷史在 [JavaScript: the first rrides the Expires header, even if the Expires header is more ree related IPC infrastructure is removed,
including preferences a類似，都是屬於權限管理的問題，而這個漏洞的輏碼：

``` js
// 正確的命名
const NS_RULES = {
  NS_GENE鑰」。

不過話說回來，為什麼我們要花這麼多�f 10 seconds of media per file seems to strike a reasonable bala的部落格的話，會長得像這樣：

![w3m](/img/js-histoLidemy HTTP Challenge 的設計以及彩蛋'
date: 2019-05-18 20幾乎都是 client-side challenge 的 CTF，我是滿喜歡的�used.

The expected solution for this problem is to make the re�我反映，感謝。

參考資料：

1. [MDN: Hoisting](http {
    method: 'POST',
    headers: {
      'content-type': 'app個 Request，是為什麼呢？

其實這又跟上面講的 2024.ctfcompetition.com") {
    throw "invalid host";
  }
  retu�所以 Chrome 的做法是正確的，但 Safari 卻把這個�ev mode enabled?
2. +36 hour hint drop: try to find a way to get��那邊去：[Consider introducing a "same-site" concept that in 寫得很清楚：

> The community platform for the future.

��講清楚，之後也新增了提示的功能。

## 第二關對應到的參數。

但是在 JS 裡面並沒有這種東西�還是先簡單介紹一下。

在 JavaScript 中，可以用 `生。

這個 key 是怎麼產生的？有許多公司可能是而已：

![upload file](/img/google-ctf-2024-writeup/p1.png)

don't need to wait for the response to come back (I don't need t
Before talking about what is asynchronous, let's first mention how to connect to the back-end API and the difficulties you may vigator-sendbeacon-64kib-and-source-code/p2.png)

這是因為剪怪的，不知道有沒有成功就是了。

這就是最後�)：

> here's a variation on the question... will JS engines exse, they can access each other's windows.

Next, if they are th��。

而 `navigator.sendBeacon` 就是為了解決這個�是這個資源：[JavaScript: The First 20 Years](http://www.we retrieve data?

The answer to the first question is obviously g](/img/wordpress-plugin-amelia-sensitive-information-disclosureharset="utf-8">
    <title>Evaluator</title>

    <sype) {
      return Object.getOwnPropertyNames(prototype)
    }
ariables like this is not very good. We can use the concept of C converting to more than one primitive type, it may use the opti

Some people may think that this example is too simple, and in der();
  const string = strings.join("");
  const hash = await c;

    await Note.create({
      title,
      content,
      use如說我們想知道 HTTP status code 與 message 的關係，� data, cb);
};
```

The key point here is the middle part:

```  so we can pass in `cache: 0` to bypass the check and make `if ( If the element is detached from DOM, the method throws an errorom CSS, CSS Keylogger can be successfully implemented. Although ecified, use this file to determine whether it exceeds the set rn is very low, and even ssh cannot be used. Therefore, the solut�文章：[Don’t break the Web：以 SmooshGate 以及 keygen  pre-wrap to a parent div, this way it will preserve spaces and 
> The second storage mechanism is designed for storage that spa/').then(res => res.json())
      .then(a => {
        setData(a��麼？
4. 如何把 same site 變成 same origin？事不宜�t provided by the server. When the password is received by the sA Complete Guide to useEffect](https://overreacted.io/a-complete. I still need to have confidence in myself.

By the way, there the principle was easy to understand. I decided to test it on acestors](https://content-security-policy.com/frame-ancestors/)--">
This is where HelloWorld.class runs.</APPLET></P>
</BODY>
</H, the latter is closer.

What I have to do is communicate with t起，接著來談談攻擊手法，以及防禦方式。

<!-- 有搞懂。

有很多人都知道 hoisting，但是理解程�抓出來，但這個沒什麼效益而且花時間，另一種�不只是單個請求，而是有個 queue，因此這樣做是IMPORTANT Functions of Yelp](https://hackerone.com/reports/30512�樣的概念滿有趣的。透過在 redirect 過程的途中�ves you explicit control over this behavior.

一開始說這不ctivity} did not call through to super.onCreate()
```

那我這個方法裡面。

除此之外可以在程式碼裡面看� const libraries = JSON.parse(fs.readFileSync('./data/libs.json'ine of code (with notable exceptions) can execute both on the clame` to ensure that the input is a valid variable name.

This ar�整段用一個沒有關閉標籤的 div 包起來，像是這�but it seems more complicated, so let's not study it for now.

#ioned at the beginning? If you think about it carefully, you d nothing to do about copyright/licenses, only about potential ma network error so we return true here. This is consistent with tw/)
2. [Cymetrics Tech Blog](https://tech-blog.cymetrics.io/)

ill be the same.

From the discussion, it seems that in React 17門去弄個 KMS（或甚至有些是沒想到可以這樣或需畫面上你就只會看到一個「C」的字元，因為我們sentation is absent, date-only forms are interpreted as a UTC ti� SSR（Server-side rendering）
date: 2023-11-27 15:40:00
catal fetch is used keepalive is the default, and Chrome only allows mise(resolve => setTimeout(resolve, ms))

function write(content>
```

The content of page2.html is as follows:

``` h       src: url(https://myserver.com?q=b);
        unicode-range��變 `document.domain` 後，兩個頁面就會變成是 same ofirst type is where you can control the second parameter of the;
```

If we cannot override cache, then we cannot use the abovtc. There are also reference examples below, which is a very com.  In other words, the Holy Grail.

底下這張圖就是所謂�puzzled about at about the same time, accidentally solved it, an has already been rendered in this element`，跟你說這個元Effect will run after the render is committed to the screen.

�# DOM clobbering

此解法來自 [@airispoison](https://twitterr needs, set the request's mode to 'no-cors' to fetch the resourre comes the last part of the challenge. The goal is to find an ed. Later, I found out that I remembered it wrong, and it is act��個可以做 HTML injection 的地方。

``` html
<div id="ht where the first page-load was always rendered server-side, and  NOTE: check again to see if it really meets the condition
  edded in the raw HTML page at any time (unlike the other flavors // fallback to linear since I am not familiar with binary searc At runtime, the contexts build a stack in parallel to the execusion, an error will occur. So if it is set and passed, it means �也有這個關鍵字的書放進去。

而下一關的 token ��為。

要防止這種行為，就是在 render 的時候要�許內容有部分的 HTML，最常見的就是部落格，因�，就能夠繞過最後的檢查。

然後隊友就發現了 e purpose of the interview is just to show off, then it is compler is that at that time I felt that I couldn't even touch the ed be executed unexpectedly. Scripting tags that can be embedded irow onerror=alert,1
```

就會先執行 `onerror=alert`，再�wever, the frame neither accepts that policy using the Allow-CSPquests to arbitrary URLs and reflecting the response back to the�就是要來決定關卡的內容要有哪些。在一開始我ay object. Therefore it can be transferred to other kinds of obj就需要回想起來有一個很好用的 [String.fromCharCode(What can we do with single CSS injection?](https://www.reddit.co		}
/******/ 		// Create a new module (and put it into the cachef 255, so the limit for wss will
// be the same as the limit foring. It is worth noting that some of them are issues caused by c�類的，這些 library 如果沒有正確使用的話，會跟 and obtain this information.

By now, you should have some sensse the implementation of the code was quite simple.

Next, let's演算法：

1. If A and B are the same opaque origin, then ret��會是字串，也不會是陣列，也不會是 0，不會是oesn't seem to be any problems, but it can actually be bypassed ps adding up, and then read this counter in JS to achieve the fu面程式碼是什麼意思（我其實也沒有真的很懂，�se Summary

Due to compatibility issues, it is recommended to us @returns {boolean}
 */
function shouldDeleteRule(rule) {
  if ( of stealing data using CSS and Scroll-to-Text Fragment feature.ken => {
    this.props.onChange(token);
  };handleLoad = () => ill be called twice in this case, the user won’t see the inteound the experience to be quite good, so I decided to write thisorigin policy](https://developer.chrome.com/blog/immutable-docum，發現確實是有資料被偷走，就有了大家看到的�改哪裡。

身為前端工程師，我覺得有個可以輕�ntext is being requested, and what happens depends on the user armation.

Below, I will briefly introduce the architecture of Am 來載入，所以有個 race condition 的問題。如果 img tive from two years ago, I realized that my concepts are still s.tw/2022/02/08/what-i-learned-from-dicectf-2022/)

## RegExp �('Server is running on port 3000');
});
```

對於一般使用�default-src 'self'; script-src 'self'
```

`self` 代表的是 sever to pass an object to any function! The reason is that Java �因為 Safari 支援 SVG font，所以不需要再從 server �   continue
        }
      } catch(err){}
      
      if (itemtr_replace(' ', '', $bad)))
        $res = "I don't like this: "     })
      )
    );
  }
  
  // Add automatic embeds to body
��：`<a href="javascript:alert(1)">my website</a>`，在使用�ne way to defend against it is not to let data from other websit�屬性叫做 `ping`，當使用者點了連結，瀏覽器就�in detail. Chapter 3.2. Lexical environments: ECMAScript impleme�道的，有關於 JS 的東西。而 I don't know React 是我 not logged in, you cannot get the nonce.

The above is the basi�結果。

如果想看實際的 demo 跟 server 端的寫法�d be familiar with this pattern. When you enter anything, the s6 萬的字串：

``` html
<script>
  for(let i=1; i<=10; i++) 是可以自動抓到一些東西，但寫清楚還是比較好�hough it is part-time teaching, I believe that the quality of t�參考這篇：[concatAll and concatMap rather than flatten and      await page.goto(url, { timeout: 5000 })
        await new m/facebook/react/issues/16382)，想看程式碼的話在這邊�
> Even your favorite developers may not know many things that y�後，再看一次上面的範例程式碼，就可以一步一e really is the charm! I've finally created an unhackable system�文章，可以讓我們試著用 node.js 把程式碼編譯成to block it, and it will be blocked. Nginx will adjust the numbesearch results because I desperately require sustenance";
    }st(str)) // true
console.log(regexp.test(str2)) // true
```

但ood']
```

當我們在前面加上一個函式時，函式的�
那這個對我們有什麼幫助呢？

我們可以自己製�eter
and (this appears on windows only because of the way it han所說的東西。

在看 origin 的 spec 時，就有提到一�� flag，最後就能拿到 flag 了。

作者 writeup：http: true
tags: [JavaScript,Algorithm]
categories:
  - Algorithm
--一套工具來熟悉就好，但重點是在這之前，你必�應用就是重新導向到釣魚網站。這樣的釣魚網站exports = {
    cal: calculate,
    name: 'hello'
  }
}
  
// �e=""><style onload=alert(1) ">
```

我第一次知道這個是�se the latest version, so there is no way to bypass it.

The authich can send test SMS messages, all have only one parameter:

`efined
```

現在的問題就變成，該怎麼透過控制上�ion to use to handle the data when it comes back.

With the abone';
```

因為沒有 `unsafe-inline`，所以我們之前的 就會把 c 加上去並且設定成 3，這邊先假設我們�otype.years",
      "Number.prototype.year"
    ]
  },
  {
    "�運作的，就是透過 hole 這個特別的東西。

# 總�```
refused to execute inline script 
because it violates the fo就沒有用到任何括號，但又能執行任意程式碼了�`，因為它們其實就是完全獨立的兩個網站，並不�的表格，大家可以仔細看一下：

![registrable domaiking, what you write is different.

In addition to the develing into considerations the state of modifier keys such as the s       const ns = node.init.arguments[0].name
        const aliae，但有可能是 schemelessly same site。

這邊其實有�ck
```

執行完之後，就會看到多了一個 `web_modules`e are a few areas I want to focus on:

First, I have a good foun編譯階段的時候會處理宣告的部分，所以會是這�可以拿到下一個 `"` 為止的 HTML 內容，把這些當佑們所謂的 timestamp。

而這個 timestamp 指的是：「�aScript]
---

JavaScript 裡面一共有幾個資料型別？又ies['command.bus'] = function ($c) {
    $commands = [
        / 基本上都是用 [react-router](https://reactrouter.com/en/ma.$to_ary",
      "Array.prototype.$unshift",
      "Array.prototked by a large numberof requests. Unfortunately, the server was ccess']

export default function(context) {
  return {
    CallE的誤差問題。

另外，上面提到的 `Number.MAX_SAFE_IN cannot be found.", 404
    return send_file(file_path, mimetypean be seen through the `info` field. Additionally, this vulnere posted the reason for removal in RemovedPackages, where we pla� ReactiveX：

> ReactiveX is a combination of the best ideaself-reflection of a junior engineer at the end of the year](http//blog.maple3142.net/2023/08/27/sekai-ctf-2023-writeups/#leakles.

Actually, I had this ability before, but it has become more agination navigation 要自己做，幸好官網已經有詳細�ion of customers.

What else can be done besides modifying booki    } catch (err) {
        log("[reassign]: ", `Error changing ed = true;
    element.innerText = "click me if you are not robogo.jpg" />
```

After refreshing and opening the console, you wredentials(m.credentials.username, m.credentials.password)) {
  ，第二個階段才是真的一行行執行程式。

在進�的行為是沒有被指定的。
2. `contact` 必須回傳一� srcdoc="<script>alert(1)</script>"></iframe>
```

由於 sandboconst user = new User(request.session.userId);
  if (request.heaeb Fundamentals](https://developers.google.com/web/fundamentals/log the correct fields of an object at the instant it is called]ize that the author got caught up in the blast radius and we lootarget)

  function bfs(target) {
    while(queue.length) {
   `

So why do we need a scripting language when we already have p�問題了，而是大家都會這樣。可是，卻有其他人說可以把任一全域變數當作 data 傳進去，可是要�些限制，解決方法也很簡單，就是在 Server 那邊圀大的差別在於「執行」。

編譯這個步驟就是把AndroidManifest.xml
├── apktool.yml
├── original
the essential skills of a front-end engineer, and I can discuss ��在幹嘛你知道嗎？
2. 你知道 `require/module.exports`.flatten broke MooTools' version of it.](https://bugzilla.mozillub.com/aszx87410/lighthouse/pull/1/files)

改完以後重新跑ch it appeared and why it was needed and designed in a certain wecute Java. The advantage of this is that users do not need to d` 或是 `2020-12-25T16:00:00Z` 之類的，簡單來說就是�/ 		// Return the exports of the module
/******/ 		return moduleged to assume that there is no locking mechanism」，要小心�就是你的 source code，只是格式不太一樣
你可以�，這是不衝突的，或是你也可以寫一個超簡單直</div>
<div id=intigriti>
  <div></div>
  <!-- !!! -->
  <div clnew Date()` 可以產生出現在的時間，然後 `new Date().NAME can lead to security issues when placed behind a proxy #265ample, many people may have encountered a strange design:

``` 在瀏覽器環境中，利用 `window.onerror`，可以接收�
from flask import Flask, Response, request
app = Flask(__name__racters. For example, the following example is taken from [MDN](��下來。

做到這邊，我們就可以讓 data 是 `<script available after navigation.](https://issues.chromium.org/issuesr();
    let signal = controller.signal;
    fetch('https://leakWhy is there this error?

In fact, for security reasons, the br, `Value of "${o.a}" was changed to "${o.b}"`, type='warn');
   velopers shared the same pain points with the traditional clientle.log('invalid password')
  password = prompt('input password'): 'reassign',
        message:{
          a: 'Z',
          b: 'icious code seems to be inside a dependency of the theme, whicps://challenge-0721.intigriti.io/htmledit.php?code='
const inser).toString("hex");

    // Surely this will be enough to protect      <iframe class=console src="./console.php"></iframe>
    ��的時間。

這時候就要按照上面所說的，用：

`-bg.png"
tags: [Android]
categories:
  - Android
---
## 前or tutoring, making it ideal for setting up a simple reservation�也很明顯，就是：

1. 我們不知道字元的順序為�的那個檔案是頁面會載入的其中一個 JavaScript，�com/file/file/blob/master/tests/escapevel.result

可以看到�N</span>
      <span>T</span>
      <span>I</span>
      <div id）。

### 輸出說明
對每一組測試資料，你應該回('child_process')` 其實就可以了，因為 require 裡面也ed`, `form`, `img`, and `object` elements that have a non-empty  the logging occurs before the key is placed in, there will be n';
```

就湊出了一個不需要括號的 payload。

## 再�racters produce a valid `charCode`, the only exception to this st.

In addition to this header, there are actually others that string 上面帶過來，我看看是不是對的。
帳號：ad

For those interested in more technical details, you will need �好了。這些設計其實並不是 JS 獨創的，而是直�

After consulting with other colleagues, it was learned that i寫過了，所以直接下指令就好：

```
npx webpack --con will be null.

In summary, the idea is roughly as above. HowevDota%202-272x380.jpg","medium":"https://static-cdn.jtvnw.net/ttve';' was delivered via a <meta> element outside the document's <writeup: https://brycec.me/posts/corctf_2022_challenges#sbxcalc��個簡報：[Temporal walkthrough](https://docs.google.com/prehrow '=alert(1)';
// Uncaught =alert(1)
```

再來，由於 thrhould we block lifecycle script of dependencies during installatode.js 的使用，那瀏覽器呢？

可以先來試試看什�個字串，後面再加上一些東西就可以執行，像這�
  // NOTE: server is quite fast so no need to set timeout
  asy有 Applicative 的值必須在它的 [type representative](httparse requirement](https://bugs.webkit.org/show_bug.cgi?id=235468做不到了，因為前面提過的 `Cross-Origin-Opener-Policy)
}

const text = {
  type: 'div',
  props: {
    dangerouslySetthis, we can see that our `GetWPUsersCommand` will be handled by fullName = prototypeName + '.' + fnName
          result.prototore -->

## 事前準備
首先，我們需要一個用來被})


    try {
        let page = await browser.newPage()

     to do harm, they can still obtain it.

One solution is to start  occurred in this section. The reference material is also though
@app.route('/', methods=['GET','POST'])
def index():
    try:
  output: {
    path: __dirname,
    filename: 'webpack_bundle.jst src="./main.js" type="module"></script>
</head>
<body>
</bodmpty now.

If you only use this data yourself, it would be a pit!-- &lt;img src=x&gt; -->
....
```

而在一些情況下，其�ion 可以把這個行為關掉：[Add --no-build-extension and ��就會出現：`Error: Cannot find module 'hello'`

但因為  a);
  }
}
```

這個 function 會加在 `Function.prototype` �the core concept and note the Discord discussion.

The core codeur payload')
value = value.replace(/[>"]/g, '')
tmpl = tmpl.replops;
    window.grecaptcha.render(this.divRef.current, {
      s在 Chrome 上沒問題）：

``` js
TypeError.prototype.name =，才能避免這種假解的狀況。

## 結論

寫這篇的�因為你必須實作 SW 才能快取檔案並且實作出離�quest is needed. 

Assuming that a server provides an API URL ca�流程，想更詳細了解的話可參考：[You Don't Know JS       logError(cachedResource.error());
        return false;
 i语法](http://blog.isming.me/2015/01/14/android-decompile-smal)V
    .locals 1

    .line 20
    invoke-super {p0, p1}, Landromessage=Note added successfully");
});
```

注意那個 `userNolook forward to what I will become next time.

That's it. See yo，需要考慮到的問題是 API，例如說如果把 API callata/issues/103)，於是他就找出了一個新的方法：

``[0-9]+}` for displaying the appointment as an example. It corres�有畫面而已，沒辦法跟它互動。到了 client 以後�ble on GitHub for those who want to learn on their own.

The secpecification.

bool KeepaliveRequestTracker::tryRegisterRequest(kit for developers, reverse-engineers, and security researchers.��

在 spec 中可以發現除了 same site 以外，還有另�。所以要嘛就是直接拿字串來比大小，要嘛就是�/p4.png)

## 安裝到有問題的套件會怎樣？如何防禦changes were made. The first is to add permission check for cust�面，就一樣可以拿到這個物件：

``` js
var obj = ree-value pairs 用的。

> The first is designed for scenarios w找到一個地方植入程式碼，那我們是不是可以想�有兩種方式：

1. URL 直接下載
2. git

第一種的話�')).hexdigest() != signature_string:
        print("nope")
            # make sure the file is a valid zip because Python's zipfipt 的檔案，但是當我們用 fetch 來抓這個檔案時�eact-dom/source.development.js";
import "/src/index.css?import";�路徑找出來。

### Final solution

最後產生出來的 pders(
        $notification->getContent()->getValue(),
        $用的元素，就會跳出來，例如說：

``` html
<body>
<面試的時候可以考那種比較簡單的題目，有時候�ar to the previous one, both of which are related to permis這個 console.php 的頁面有一些條件限制，如果沒有 the system, and may later be embedded by the vulnerable system Blocking methods execute synchronously and non-blocking methods ers cannot see these APIs on the front end, but the code of Wordre not necessarily "keys," as the term "key" specifically refersner.frames['f']`.

However, the biggest problem with this questi, it should be reminded that to achieve this kind of attack, the�會知道是怎麼一回事了。

話說這個 issue 的作者y don't we just use element IDs as identifiers in JavaScript?](h��在 config 裡面加上 `process.env.TZ = 'Asia/Taipei';` 來�題大概就是原先用 `<img>` 的一些好處會不見，像�lled the runtime environment, and the most commonly used one is:tps://book.hacktricks.xyz/pentesting-web/reset-password)，除�門之四：使用 Frida 進行動態分析](/2023/04/27/androiinstructions and tried to make them as clear as possible, and al跳轉到我們準備好的網址，變成這樣：

```
- exp.h誤的話，還是有可能存在 XSS 的漏洞。

前陣子我e can use importScripts function from API to import external scr��做得到的，而且 cookie 會自動帶上。因此只要是好，看到這邊相信大家已經知道了兩個重點：

1.
```
[generated bytecode for function: find_me_let_for]
Parametein 的 XSS 才行。

而前端的部分可以設定 cookie，�hat it is more like a transitional thing, and its function will at the rule checking is not recursive; it only checks the top leres.setHeader("Content-Type", "text/html; charset=utf-8");
    rin2.close()
      win.location = URL.createObjectURL(new Blob([`r are filtered out, so other people's data cannot be seen, and p).GetString()));
  }

  request.SetKeepalive(true);
  request.Set` 會回傳的格式，它就會被看成是一個 React compons from rivers.

Recently, I went back to review the reports fromciple behind it, you won't be able to solve this problem.

The t，這也是最常用的表示方法。

所以，若是你實�,627 (that is, 2^64 - 2^53 + 3) values, representing the double-```

有一個很明顯的 prototype pollution，要做到 RCE�cs.io/posts/huli/prevent-xss-might-be-harder-than-you-thought/)�ically mention this more precise definition because it is very c起來的，只要任何一個小套件被攻擊，在安裝的�.js 內容如下：

``` js
(
  function e(t, n, r) {
    functihAll called with a non-global RegExp argument
*/
```

出現了�the program to receive the response smoothly.

If you carefully  cookie.

But that day, I found that no matter how I wrote it, Ihich has the following code:

``` js
if (opts.client) {
  src = equire("express");
const cookieParser = require("cookie-parser")la.org/zh-TW/docs/Web/JavaScript/Reference/Global_Objects/BigInt/2021/02/19/cors-guide-1/) 的限制，這些都是瀏覽器這�分合理。

不，還是不合理，請看底下的圖：

![presents the actual "character code" and is safe to use with
 * iends and some adjustments and improvements, I let my students tist`下面找到一個 apk
要注意的是這個 apk 還沒有�)
    currentIndex++
  }

  // add eval structure
  // generate rces, one of which is the current Document. There are two kinds 

Because the system uses the public key for verification, and t the answer directly, so I must provide a way for them to see thon body of that function. This is useful when the name of the fu_origins)

最後，希望這篇有讓大家更理解什麼是 s1460829.html)---
title: '從 GitHub Issues 搬回 Hexo'
date: s://webhook.site/d485f13a-fd8b-4cfd-ad13-63d9b0f1f5ef" />
```

�3. 讓 admin bot 訪問 id 是 0 的 note
4. admin bot 被導到ich is something I didn't think of at all.

I have set `app.set(不錯，但也發現一些問題，其中有些我上面已經檪成功是：`/?m=your search...at 1651732982748 has success...et, everyone naturally wants to get a piece of the pie. In late is divided into two parts, one is the mailbox, and the other is f (!safe){
                data = data.replace(/</g, '&lt;');
  �方法，第一個類似於 [SekaiCTF 2022 - Obligatory Calc](hse
    };
  })
}

setImmediate(run)
```

沒錯，就是這麼�m-control"
							placeholder="e.g. I learned how to use `templaLIA_ACTION_SLUG is replaced with a blank space, and it becomes `me? Is this feasible in practice?
3. Is it possible to steal thity r2, [1], [4]  // 載入一個 r2 的 Property（應該就是 r things and haven't had a chance to participate in a CTF. This ,
  a: undefined,
  bar: function
}
```

### Line 2：var a = 1
SP bypass - MooTools</title>
    <meta
      http-equiv="ContentJS 相關知識

### DiceCTF 2022 - no-cookies

這題的重點�b": "556683653781741"
}
```

Although I don't know the internal struction

The first step is to generate a secret key and ensure�吧。
```

This level tests two things:

1. Whether you can pa� `.npmrc`，內容是：

```sh
git=./pwn.sh
```

然後再新�可以統一，在基本的 coding style 上面會長差不多�s fine in Chrome, so we can change it to the above code to suppuse there is no "A" on the screen, nothing will change. But when&& module.__esModule ?
/******/ 			function getDefault() { retur�，而且還可能會有 unintended。

接下來就講一下 M舊的情況之下，執行 JavaScript 還是需要一段時間�後可以再寫一篇文章來補齊沒有講到的這些部分就是利用 [connection pool](https://xsleaks.dev/docs/attacksttps://zhuanlan.zhihu.com/p/28590489)。

先來看文章裡面�urry.call().alert(1)}}

</div>
```

It requires no user interactplies the noopener keyword behavior under the same conditions.

member when using Heroku, after creating an app, I had to downlowindow object

Here is the final payload for the intended solureturn new ActiveXObject(
      navigator.userAgent.indexOf("MSI first open the incognito mode to see if your browser will be in�參考我之前寫過的[淺談 DOM Clobbering 的原理及應� 針對上一點，可以用 iframe srcdoc 來繞過（但一般mponentDidMount() {
    // 檢查是否已經載入完成
    if  
> Can you create something out of nothing and read the flag aMS Service Abuse and Sensitive Data Disclosure

Let's look at th incorrectly. The third article will explain in detail the detai``

One thing to note here is that if the user is `null`, they wmy opinions on this issue, and then the recent [I know you under�以，其他就要付費版才有了。

寫到這裡，我們�d from a single domain at the same time. Therefore, if you use o�同的情境之下，可能會需要用不同的方式跳脫，要提的案例還滿不錯的，如果能夠理解這個案例� don't think we are ever going to fix this one. We can't clone o, renderOptions, done);
};
```

Finally, `tryRender` is called, ��？

若是沒有明確地定義輸入以及輸出，其實你�能會有不同的輸出。

因此 Server 端給你什麼都�sion-attacks/)
4. [Understanding the length extension attack](ht�後的那一段。

先從最後一段開始好了，這個 `th�我們的 XSS payload：

``` js
const challengeHost = 'http://o very different things. The former will have many restrictions,# 找出 cdnjs 上所有的 library

去 cdnjs 的網站上面�l.

However, after visiting this website, I tried to go to `httpi.tw/2017/08/27/ajax-and-cors/)，裡面把碰到的問題與解的 Cache 並不是我們今天的重點，有興趣的讀者們lated logic themselves, so they will redirect both actions to thrlier, the pricing model allows you to calculate whether it is mowing if I can do it" to "I can do it, but it may take some timeferred from the fact that "clearing the cookie will solve the pr  if (!users || !password) return false;
        for (x of acces官方的文件：https://nextjs.org/docs/pages/building-your-apreates a new closure for SharedFunctionInfo at position |index| ter-key is technically both printable and non-printable and can
one to update to `#0`, one to update to what we want, and anotheside-effects of e
  // being converted to a string) followed by title: RCTF 2022 筆記
catalog: true
date: 2022-12-14 20:10:44
�最主要的目的只是想讓你知道：

1. 為什麼要用 blank page will still be seen.

Although this can achieve more c``

就只是一個 style 有著內容，沒什麼，但是用 `   --d: var(--c),var(--c),var(--c),var(--c),var(--c);
      --e:after clicking the button, a popup will still appear for you toetjs/sheetjs)，而[官方文件](https://docs.sheetjs.com/docs/��)
```

寫程式的時候也是這樣子的，我們常常會� = Java.use('com.cymetrics.demo.MainActivity')
    MainActivity.
app.get('/leak', async (req, res) => {
    leaks.push(req.queryion files (although I have only tried simple projects and cannot�為例，其實只要在讀取輸入時把字串都轉成數字�果是更複雜的例子呢？例如說你想要找「李X明�f you really want to open up the style, you can also use CSP to rowser does not block cross-origin APIs, then I can write a piec�如說假設某個 subdomain 有 XSS 漏洞，就有機會利�="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet"ontent script is loaded."), n = (0, i.default)(location.href), otioned that many companies have no issues with key storage, but log happened to want to move, so I used the one I had previouslyy step with an issue could lead to key leakage.

In the case of embed code that will be executed on the backend and output the r 開頭，但後來發現這樣做比較有趣，而且如果不要想辦法在本地弄出一個檔案。

解法是在新的 hdirect or transitive—can include .npmrc files that override thlocalStorage.getItem('dark-mode')) {
    if (localStorage.getItethis CSS, and the contents of the script will be displayed on t根據 [Why were Javascript `atob()` and `btoa()` named like tha the object in |slot_index| of the current context into the accuer is present on the requested resource. If an opaque response set the data to be returned. Finally, the result is returned and hem all when I was a child, I gradually forgot about this type oeter that compiles your source code and then executes it.

In faxample to see what it looks like after compilation:

```y，在我們的例子 entry 只有一個
    所以可以簡單rset="UTF-8">
  </head>
  <body>
    <bgsound>123</bgsound>
    大多數都是跟抓使用者資料的那個小程式有關，�04/introduction-mvc-spa-and-ssr-545c941669e9/)，那時候的我�文章簡單講一下這個主題。因此呢，這篇是給完alled through `wp-admin/admin-ajax.php`, and the relevant code e
const fs = require('fs')
const crypto = require('crypto')
const
> In Fiber, we create B first, and only later unmount A and mou�來就是 `Uncaught=/`，這個 `/` 其實是 regexp 的意思�ent understanding of JavaScript (and you will also learn a lot result when searching for "world", I also found several other bo�幹嘛，我參考了原始碼：[browser-pack/prelude.js](http Architecture](https://jasonformat.com/islands-architecture/)，所以沒辦法 XSS。

目標是偷到 admin note 的內容，�oints for each level are different. I will share it with you whe��剛的`smali`裡面，有這麼一段：
``` java
.line 15
�，通常我們會抓一個合理的誤差值，意思就是�nts not yet inserted into DOM -->
<svg><svg/onload=eval(name)>
`
Regarding the second solution, do you remember that we wrote a 已。在這種情形底下，你寫一個把字串轉換為數�      allow 127.0.0.1;
        deny all;
    }

    location ^~ �

> While creating a new auxiliary browsing context, the sessioo still use the old API, so I added it to avoid confusion.

If y��個：

1. `module.exports`
2. `require`

前面有提到過�以在網頁上跑一個應用程式起來，就是 Java Applet�],
      "entityRanges":[],
      "data":{}
    }
  ],
  "entity);
  };

  useEffect(() => {
    handleLoad();
  }, []);

  retu="01"]){ 
      .i01{ display:flex; }
    }
  }
</style>
<div cl�資料庫查這個套件有沒有問題，有的話直接攔下��每一個屬性是否符合我們想要的規則，並且回�   res.end();
    break;

  default:
    res.statusCode = 404;
 er problem?

We can first reduce the width of the div to only dint type，所以瀏覽器會優先看合法的 `image/gif`。

�
不過重點其實是第一個，因為瀏覽器原生的 ES6 �ginning, without any links or fancy things. It's just a plain tesed, but upon further thought, I realized it was not possible.

�本的理解是很重要的，若是你完全不會的話很建�趣的，會去測試一大堆元素的 interface 是不是正�tly from my computer using curl, I will only get an error becaus�前端，發現了一個奇怪的現象。

當你在開發者�t-Type: text/html\nConnection: Close\n');
    const json = proce"undefined" src="src">
</form>
```

But, it's not working becausdf.s3.amazonaws.com
html-translator.herokuapp.com cdn.optimizely#L89)：

``` js
export const optimizeEmbed = (content: string) em."

Even after leaving the company, they still know more intethis challenge initially, it was obvious that CSS injection was �能看到完整的畫面。在等待的期間，畫面就是一problem, so I will note the location: https://source.chromiOC</title>  
</head>

<body>
  <script>
    const htmlUrl = 'httent 的 render function，就這樣一直往下執行下去ﻥ預期的狀況會是最剛開始的樣子：

```html
<iframe還原的時候通常都會經過字串操作，這時候我們�果你不知道什麼是 XSS（Cross-site Scripting），簡�These are links to resources that are to be used to augment the h will give you a better sense of this attack.

### Yelp

The larticle only mentions a small part of the history, and ther the whole paragraph is only the line `LdaGlobal`, which seems 22/08/21/en/corctf-2022-modern-blog-writeup/

### HITCON CTF 202n || article.content),
    }}
    onClick={captureClicks}
    re /img/dive-into-npm-supply-chain-attack/cover.png
---

2026 年 ，server component 在這點上就滿類似的，藉由把頁�').innerText = token
    }
  });
};
```

好，這個 reCAPTCHA       typeof elm.insertBefore !== 'function' ||
            type��面，真的能夠順利執行了！

上面這段程式碼只ithub issue, nodeBB, discourse](http://huli.logdown.com/posts/19om/web/updates/2018/03/smooshgate)
4. [Non-conforming features](       head: null,
        body: null,
        flag: !1
      on/x-www-form-urlencoded`, `multipart/form-data` 跟 `text/plain not support `ALLOW-FROM`.

The solution is simple, just add CSPer.

In other words, if we are not using a browser, if we are nos">Settings</string>
    <!-- Strings used for fragments for navion to decide what to do with with a given mime type. This takesponse is.

## Summary

This article mainly discusses why browsOrigin' header on initial XHR request](https://serverfault.com/q�試了一下似乎是沒有找到，因此才出現這個比較
我們找到了兩個漏洞：

1. 透過 `<iframe>` 把使用�teHash('sha256').update(token).digest('hex');

app.get('/', (req a mysterious thing. You just need to pretend that you are a JS �制比較多，例如說內容只能固定等等。我們自己.tw/2020/12/01/write-conosle-log-1-without-alphanumeric/) 那篇ll be used.

Luckily, we can make it happen by leveraging the pr a place where ESLint might be suitable, so I tried it out and f   if (results.length > 0) {
          return resolve(false)
   values = []
      for (let item of list) {
        values.push([``` js
#!/usr/local/bin/node

// flag in ./flag.txt

const vm = You are not allowed to send test email');
}
```

## Conclusion
basics. As long as you declare variables properly, even if you d�項目或是開發時會用到的套件，忽略了這些也都ope chain. A scope chain is a list of objects that are searched ore, this approach is likely wrong and not feasible.

So, what oan EC (you can think of it as executing a function, but before r�果有 API 的話會變得有點奇怪，如上所述，初始�ent, the keydown event is fired for all keys, regardless of whetde will output `function` instead of `undefined`.

Okay, the ba�什麼需要 webpack？

問得很好，看到這邊可以先�idate’s knowledge in assessing operating systems and common ner.com/payload?len=1` will return a response, which is the payloade.js v8.17.0, so the output of some situations may be diffe��過了 custom header 的限制。

整題更詳細的 writeup   for (let i = childCount - 1; i >= 0; --i) {
            parent 25 筆資料；page 換一下之後發現沒什麼作用，於�奇：

``` js
function isChinese(text = '') {
  // @see: httpsetify/You-Dont-Know-JS/issues/160)
10. [Parameter passing in Jav     script.src = url;
            script.onload = () => r(false個幾天會比較好。畢竟不能被利用的漏洞風險可�跑任何東西，但因為所有東西都變成 `undefined` 了�一直不斷嘗試才能試出來到底是什麼樣子（例如d passes the entire JavaScript object to the Function, so you ca the opportunity to speed up the loading speed of requests and ook so much time to build, indicating that there are many areas act, I learned a lot from this incident.

The first point is tha可以建立一個 post，內容可以控制但會被 sanitized�   if (username === null) {
            fetch(`/share/read/${id}se two lines. When we write the first line, we only need to knoone 等等，都沒有這問題嗎？如果有的話，他們的�字做一些「不是數字」的操作時，就會產生一個ub.com/nodejs/node/blob/v18.8.0/lib/internal/modules/cjs/loader.��是盡量讓自己寫出有漏洞的網站，然後自己試�nly look at the URL, it basically has to look exactly the same, /img/sekaictf2022-safelist-and-connection/p2.png)

如果 note �`` js
if (
    req.headers['sec-fetch-mode'] &&
    req.header:

``` js
[][`${``+{}}`[5]+`${``+{}}`[1]+`${``[0]}`[1]+`${e}`[18tml (basically the same shim as POSTVIEWER V3).

The method of cs/tree/master)。

[這一篇](http://engineering.dailymotion.� [redux-observable](https://redux-observable.js.org/)，是一�, so at first glance, you might think it didn't hoist.

We will  /**
     * Implemented safe filtering of base64-encoded signatIf-Modified-Since: 2017-01-01 13:00:00
```

假設檔案確�entals/payments/payment-request-ux-considerations)，有 65.9% �ld where third-party libraries can be loaded, cached, and sharedat it will think both are `true`, right? But the answer is `true因為 scheme 不一樣
3. `http://huli.tw` 跟 `http://huli.tw:��很容易就能反編譯並且看出在做什麼，用 native 一個章節就在講這個：[Network security configuration](hnything like that! Even if there are, `csrftoken` is definitely ://www.google.com/recaptcha/api.js?onload=onloadCallback&renderlet arr = []
for(let a of chars) {
    for(let b of chars) {
   c')) // false
```

第一個 `isNaN` 是存在於 global 上面�ple.com')

// NOTE: we will calculate this baseline before";
CertificatePinner certificatePinner = new CertificatePinner.Be code but not in the language file.

It is not difficult to wri甚至是你其實根本不需要宣告一個 function，直接�b.com/material-theme/vsc-material-theme/discussions/1304)
2. [Prxss-challenge-author-writeup.md裡面有一個部分是用 web will call the `__express` method inside the view engine, and this}
```

When calling `sanitize` without `options`, the default vadration 的一個小問題，那就是在 hydration 完成以前68.tmp/base.apk: Attempt to get length of null array]
```

這昚

1. [How Are Function Components Different from Classes?](httpFQjCNHyq6urHn6HLwj8RP09GANAlymZug

So I thought it was impossiblaid to HTML, handling some basic logic, and can also be used wituld *not* be updated if they domain-match and ignoring the path
�所以若是沒有 lockfile，你每次都會裝到最新的 `turl, {
    waitUntil: 'domcontentloaded'
  })
  
  // 執行準�.log(response.data)
}

run()
```

讓 x = `__proto__`，y = toke:1 "/account/posts/294037-javascript-redux-basic-tutorial/raw"
Vng)

You may wondering why this works, how can it bypass the `ht�做的事情又更多了，效能也會愈來愈好。

這篇章：[我知道你懂 hoisting，可是你了解到多深？](htfferent, and the back-end has not added the CORS header. There下第二種可以確認是本人的機制，這樣就算被打�ted.has(obj)) return
    visited.add(obj)
    const list = Objec裡面沒有直接把 byte array 轉成字串，這邊提供一�
  let visited = new Set()
  let result = []

  findTarget(scopes: [Web]
photos: /img/everyone-need-a-http-proxy-to-debug/cover.     replacement_response = requests.get(replacement_url)

   `x`跟`y`的記憶體位置，而我們在呼叫 function 的時抓下來，以及使用 headless browser 幫忙驗證，我們�，或者是使用者手動把 alt 移除掉。

所以最後�網站的登入是怎麼做的。

因此呢，我就直接去�tps://pjchender.blogspot.com/2016/03/javascriptby-referenceby-va://www.ecma-international.org/publications/files/ECMA-ST-ARCH/ECs://news.ycombinator.com/item?id=15272374) 上面看到了這一，看看能不能改對：https://codesandbox.io/s/react-hook-cntifier associated with the key pressed. In most cases, the vag the width and font-family continuously can be achieved using le.log(obj.cal(30)) // 9
console.log(obj.name) // hello
```

要ne within KMS.

In simple terms, it is about isolating these keynally, I wanted everyone to really play the guessing game, and  It is better to write another article separately, so I won't m��，然後把所有追蹤者再丟到 queue 裡面，這樣程�，一定會有誤差產生。

有關於其他細節，我之�自己實作在檔案裡面。

什麼意思呢？我們直接TML = '<img src=x onerror=console.log(1)>'
console.log(2)
```

要 open source，是時候整理一下程式碼了，於是就�g (if you close http connection without reading you have leak inupang, the attacker directly obtained this signing key, allowinit is still the admin's session, so you can directly get the thi：`Cache-Control: no-store`，代表說：「我就是不要任tion context，然後從頭到尾都只有這一個 context，�66a03/.well-known/jwks.json`，而 backend server 看 `transfer-預期解為什麼可以 work，為了方便理解，我把它�anitizer，但並不會針對自訂的屬性。

也就是說，use it does not produce a tab-character in browsers.
 *
 * @parareturn DOMPurify.sanitize(path);
}
```

雖然說是 DOMPurify�.com/terjanq/status/1576605101514313735)，他用了 `<script>` eryone try one by one, without any other methods.

The hidden Ea想成是：newRequire(2)
  */
  for (var i = 0; i < entry.lengt��樣，是因為剛開啟之後會有一小段時間，開啟�一件事，那就是：

> Function component 的每一次 rendveral different ways to write it. One way is to use it likeimport React, { useState, useRef, useEffect } from "react";

let lines.slice(1).map(item => item.split(' '))
let min = Infinity
ult is like this, there must be a reason for it, but what��實不太精確就是了，可我一時想不到更好的了�.png)

So what's the benefit of this to us?

We can create a uni，那你就錯了，這其實是許多程式語言共同的問鬡 render 裡的 function」跟「第二次 render 裡的 functi
```

程式碼來自：[makeToast.js](https://gist.github.com/m.tw/2018/11/10/javascript-hoisting-and-tdz/)裡面有提過，�dding notes. You can see all your notes on the /notes page, and rstand what hoisting is, you must find the answers to the fo�就是被預設的 filter 篩選掉了，點 Filter settings �，之前我有寫了一篇：[如何不用英文字母與數字ng the file, the hash value on the URL will be updated, an immediate thought is to use cookie tossing from another domain,fth issue is that some optimizations seem to have a problem. For�注意到的部分，如果真的被型別搞得很亂，也可 Shadow DOM](https://blog.ankursundara.com/shadow-dom/)

第二�灰的我想說那就乾脆放棄吧，研究看看有沒有其�earlier. After the iframe in `shim.html` finishes loading, it se       Return 
Constant pool (size = 2)
Handler Table (size = 0f `srcdoc` is the classic angularJS CSP bypass payload we mentiov className="App">
      <ReCAPTCHA onChange={setToken} />
     ial Theme has just been removed from GitHub and VS Code due to sVIKREQUEST_ALLOWRAW);

//check if the signature has been submittck is the event loop anyway? | Philip Roberts | JSConf EU](httpss way, we can know what the first character on the screen is.

Aefined, while the declaration of let and const is not initializere on best practices when writing code, such as declaring variab��直接存成一個字串而不是 date time string。

假設�ist to leave comments on, it was added later.

## Conclusion

I 
We have already covered a lot of things related to hoisting, ane sandbox id=f src="data:text/html,test1:<script>document.writelt a frontend problem and cannot be solved purely by the frontend��們公司用 PHP 做 SSR」。

有點像是我問我朋友他(/img/javascript-number/sort-compare.png)

所以如果你要排sor = (
  tag: string,
  html: string,
  options: { [key: stringto the two articles attached above.

## Defense

Finally, let'sill be more things to make you even more confused later. Let's a
    } else if (char === '.') {
      addFunction('Math.PI.valuenetworkidle2" });

        // restart browser, which should clos obj = {
  a: 1,
  [Symbol.for('hello')]: 2
}
for(let key in obj-10-solves

### corCTF 2022 - sbxcalc

``` js
var p = new Proxy(�學到了很多以前完全不知道的技巧，覺得自己對 的作用以及參數，而我認為其實參數講多了容易http://dmitrysoshnikov.com/ecmascript/chapter-8-evaluation-strat Java code.

Then we open the terminal, go to the directory of te`來達成這個目標。

可是呢，還記得剛說過`no-lback 綁定到 `handleCallback`，在裡面再去呼叫 `onChan-Origin Resource Sharing，跨來源資源共享）。

你必�th fetch combined with keepalive. In fact, the underlying implem��功機率，可見：[Notes XSS Challenge Author Writeup](http stack 的感覺，但好處就是自己有完全的掌控權，new URL(url).hostname // 取出 hostname
  return host.includes(ext which results from following the hyperlink will not be an auwebpage has a more complete example:

``` js
/*
Mastering JavaSc因為 Node.js 這個 runtime 並沒有提供這兩個函式。
/ 或者是其他網路上的資源。

接著，我們就來用just a matter of comparing two numbers, but it's not that simple{
  
    // 印出目前抓到的文章數目
    const count =        StackCheck 
  188 S> 0x25d2f37dbb2b @    1 : 13 00 00    //chromestatus.com/feature/5553640629075968)，或許這就是�emented-in-all-major-browsers-advances-on-the-recommendation-traly, one day, I wanted to search for an article using keywords su調查結果](/img/server-side-rendering-ssr-and-isomorphic/p1.p     loading="lazy"
        />
      </picture>
    `
      t-wide search for information."

If you're too lazy to read it, second line of defense is breached, the third line of defense is��個時間點以後有更改的資料。

直接舉一個例�t";

const baseUrl = "https://hn.algolia.com/api/v1/search?query�然後你就有了一堆不同的 IP 可以輪流使用。

�at what the output byte code looks like. Before explaining, you / 3);
const arr1 = arr.slice(0, third); 
const arr2 = arr.slice(thor's writeup is here: [https://github.com/google/google-ctf/trsimple because those rules are clearly written on it. 

ECMAScres not mean that JavaScript cannot have a compiler, and vice ver of course platforms.

In this post, I will discuss the backgr�實案例，我自己碰到過一兩次，後來才發現原來s.constructor('alert()'))
```

## 5. 拼裝起來

我寫了一�w.open('http://localhost/share/read#id=1&username=${username}')
uli.tw`. As you can see, the path `/abc` does not affect the orub.com/cure53/XSSChallengeWiki/wiki/H5SC-Minichallenge-3:-%22Sh*
.end method

.method public onCreateOptionsMenu(Landroid/view/Mers using built-in fonts. How can we do this? We need to find twu6g?file=/src/App.js

When you open it and right-click on the el�：`node --print-bytecode a.js`，只要加上`--print-bytecodeef connect():
    global ready

    print('connected!')

    # f}

    console.log('Error, please try again')
    return
  }�」。例如說當使用者輸入文字的時候，就應該抓//update the signature in the DB
    $q = "UPDATE `#__vikbooking where it depends whether
  // we still wish to accept them (or h hexo, the high-performance template is relatively simple. If yction, which is a bit more complicated, but we won't worry aboutid App Reverse Engineering Part 4: Dynamic Analysis with Frida"
關的資訊，瀏覽器不可能讓你做這種事情，所以�ion，讓你繞過這個機制，存一份備份在這裡：httpr] === true) {
      isWin = true
      break
    }
  }
  if (iseName` is the name of the cookie to use as a value for xsrf tokelutions, so I wanted to record them in an article.

<!--      "String.prototype.$intern",
      "String.prototype.$to_symess you leverage another functionality.

### reCAPTCHA to the re來可以用這個指令：

``` shell
objection patchapk --sour打開一個有設定的頁面，我也沒辦法 access 它」

 relationship is visualized as an HTML tag, it would look like t a program. Since I cannot read it with a program, I cannot passlowing figure explains the relationship between them very clear       while ((currentNode = nodeIterator.nextNode())) {

     bs/mootools/1.6.0/mootools-core.min.js",
    "functions": [
    (),
  x => x ** 2,
  String
]);

for (const value of elements) {n error.

But if you write like this, something magical happens:ub.com/tc39/proposal-decimal)，拿來處理小數用的，就�nge. After testing, it was found that you can only have around 1�證

若是這種類型的狀況，那當然是有沒有加密�um.com/@hulitw/front-end-learning-path-55201571ecfe)，順便在個看起來不難的：[Weird image failed rendering in thread 'none'; base-uri 'self'; style-src 'unsafe-inline' https://unpkgapi`，出現的第一個搜尋結果是最無用的官方 API�t {
  listRef = null;
  previousScrollHeight = null;
  
  compon�只要能抓到「中文使用者就好」。

那要怎麼樣�wever, my teammate found a vulnerability in this part:

``` py
f�完 `npm install express` 之後，除了會下載套件以外�nction loadTest2() {
    f.setAttribute('sandbox', '')
    f.srcCombining multiple Http streams with RxJS Observables in Angular/code.jquery.com/jquery-1.12.4.min.js"></script>
  <script>
  ticle is helpful to you. If there are any errors, please let me 件很齊全，有一連串的指示告訴你應該要怎麼做㽢式。

好，講了這麼多 base64 的東西以後，讓我幞過 CSP，這題最後是用 `<base>` 來改變 script 載入�7-review-the-classical-sort-algorithm-with-javascript/raw"
"/acctiating a command, it is passed to the commandBus for processing and many people have not noticed this.

I didn't notice this attackers from injecting what they want into the website. The cor就代表有打包成功了。

所以使用 webpack 的好處�幫你做處理，都是因為有 image loader 跟 css loader 牺的值。

解法就是用 batch query + alias，一個請求� of which ticket the user was buying, then as the user clicked f<div
      style={{
        background: "black",
        color: 。不過儘管如此，我還是不知道原本為什麼會這�cle is only an overview of the general direction. If you go deep://www.facebook.com/story.php?story_fbid=1060924756061613&id=100ector('input[name=plus]').addEventListener('click', () => {
  g not found');
    $mainframe->redirect('index.php');
    exit;
use CSRF to log in and execute XSS on the admin.

If the same-si     start()
    })
}
```

In the loop, it assign `theme[themeNatags: [Others]
categories: [Others]
photos: /img/update-blog-wit await sleep(3000)
    } 
  }  
}
```

上面就是我們按照�rm').onsubmit = e => {
    e.preventDefault();
    const code = ，透過 `this.onCreate.call` 可以呼叫到原本的實作，ust follow the creation of FormalParameterList properties.

For  時，有些 API 是這個語言本身內建的，例如說 `JS接著下一行 `Star r0` 就是 `Store Accumulator to register 話，叫做 session history 以及 policy container，iframe �
  if (type === 'TEXT') {
    // workaround react-mentions bug b言的實現，那到底有沒有一種明確的定義，能夠�ngth) return this`. And we call this function without providing ss since about half a year ago, and support officially started ior. I guess many web pages used this trick in the early days to aught SyntaxError: Unexpected token 'throw'`. This is because `to-cache)）

如果要「完全不使用快取」，是`Cache-去做其他事情，而不會被綁住。

聽起來十分美�ort(data) {
    console.log(data)
    // TODO: change to your VPu send a request from `https://huli.tw`, then the origin of thiignment inside `[]`. After the assignment, it is equivalent to `所以在我們不知道 key 的狀況下，照理來說我們�userId) {
      console.log('no data from queue, end')
      bre：「抱歉，又要加一個新的需求了，老闆最近買�ong as the host belongs to the same parent domain, it is basica to a phishing website, and so on.

To prevent XSS, it is necessar details = {
    displayItems: [ // 購買的品項
      {
    move-result p1

    if-eqz p1, :cond_0

    const-string p1earned about this feature after writing React for so long, and twing order:

The most essential sentence is "On entering an exec`

Python 這邊主要會根據 cookie 內的參數來 render �t moment = require('moment')
  
function format(time) {
  if (!teaching itself is also a two-way process. In the process of teac�可以的話，那就方便太多了。

### 第三個例�efined in the HTML spec we see today, but in another URL spec, sit is a prime number, unless you find someone else's code for de[Full account takeover of any user through reset password](httpsntered this issue in the past. In 2018, it was discovered that S�不會顯示年份，否則的話就會顯示出發文年份。�（或只要有辦法換 IP 的話就可以）。

後來因為詳情可參考：[MDN: Reasons for delays longer than specifiedmethod was to make everyone willing to tell him the information,�你的 call stack 太深而且東西太多，再加上這個過�外一種思路來解決問題：

``` js
const source = [1,kage attacks by asserting that a particular cookie should only b�[Sneak Peek: Beyond React 16](https://reactjs.org/blog/2018/03/�第二點可能有些人會很疑惑，想說 MooTools 都是�``` js
return String(process.mainModule.require('fs').readFiprocessHash, true);
```

There is an `await sleep(0)` in the mil file, so we just need to modify it.

After seeing other writeude 繞過，但問題是有了 pollution 之後可以幹嘛。

 html 加上相對應的 class 就好，就如同我上面舉的hat contains the same objects as Scope.

就是說你在建立 fode, the global scope will add c and set it to 3. Here, we assumone thing I am curious about, which is how to verify that the CO�文章全部翻一遍，流程是：

1. 列出所有文章的木 animation 開始時會觸發的一個事件，而 animation �轉換，最後變成你想要的答案」，這點就是 React，主要是這一段：

``` js
// insert record
const { code } }
    catch (e) {
      alert(e?.response?.data?.message || e.mHow Browsers Work: Behind the scenes of modern web browsers](htt一步就是找到資料來源。只要這一步完成了，其� value is pressed down. Examples of keys that produce a characgest('SHA-256', encoder.encode(string));
  return arrayToBase36(GitHub Pages. In fact, the blog you are currently reading is alsnction`alert(1)` ``
```

Since `alert(1)` inside is a string, th新下載。

這邊也可以參考 Google 提供的圖片： [HTTP 1.0 spec](https://www.w3.org/Protocols/HTTP/1.0/spec.htmlprops，不會因為 props 改變而改變。這個就是 functi�打完之後的 250 ms 才發出 request。

解決了第一�o encounters CORS problems will come to see this series, and afthought of this character and logo. I even thought of the ChineseML` can be controlled, and any value can be passed in to form les is what I just mentioned, different methods are needed to escSince files are deleted after being uploaded, you need to exploiwill cause a lot of inconvenience.

Do you remember what I said ��這個 Observable 裡面的那些東西啊！現在這個情� "Replace with your own action", 0).setAction("Action", (View.Ontion="https://small-min.blog.com/delete" method="POST">
  <inpu那一串放到網址上面去就好了，變成：

```
https:/ is an alias for either `keyCode` or `charCode` depending on theority. Actors with differing origins are considered potentially �試看：

![](https://static.coderbridge.com/img/aszx87410/47eom/getsentry/sentry-javascript/pull/9416/files)：

``` js
{
  t� React 與 Vue 本身的心得，而不是特定的函式庫或是只要在 request header 裡面加上 `X-Forwarded-Host: abc.�子才看過但還沒更新到文章：[Code Vulnerabilities Pu��然乍看之下一定是物件，但如果 postMessage 之後�ub.com/project-sekai-ctf/sekaictf-2023/tree/main/web

### Scanneonent 裡面直接呼叫 API」這件事，而是在說比起在記'
date: 2016-11-26 10:20
catalog: true
header-img: "/img/hChrome devtool 的 console 執行以下這段 code：

``` js
foeout() {
  for (let i = 0; i < 10; i++) {
    setTimeout(functiol connectivity is restored. If you specify a function here, you as really brought a new way of thinking to many people and has denge:

1. CSRF
2. bypass CSP to perform XSS

The second part is �知識，寫成這一系列的文章跟大家分享。

先說� alert("🚫🤖 Congratulations, you're not a robot!");
    leave a message to communicate with me.

References:

1. [CSS I
```

I came up with this solution after the challenge started, sites and then use similar methods to break in.

Furthermore, ifwe can inject any HTML into it (but it is useless to directly dmain screen, just like installing it in your phone, becoming no -debug/p3.png)

然後那顆很顯眼橘色的「Open Browser」o get the correct answer.

After trying and fixing it for an hou`$_SESSION['name']` is not encoded before printing, so we have aving problems. Let's first look at a problem, which comes from `(11,12)` 跟 `(2,3)`，根據題目的敘述，應該要先輸jax({
        url: 'http://en.wikipedia.org/w/api.php',
      ��更改畫面內容的，就算有，也是一些比較無關�-> Options 裡面新增 Proxy Listeners，bind to address 的部rot</h1>

<p>Step 1: CSRF the admin user, to set a super long tiport` in ESLint to report errors. The message is the error you wT, we can quickly write a simple and usable ESLint plugin.

## Ceer, we can pay attention to these potential bugs and see if thessues, and the two are conflicting.

From my personal point of ve are three pages and a bunch of `postMessage` and `onmessage` e程，再繼續往下看之前你可以想一下，有沒有什�t runtime built on Chrome's V8 JavaScript engine.

所以 JavaSce
2. fetch
3. performance
4. URL
5. setTimeout
6. setInterval

#還是覺得怪怪的。原本想留言，但是後來覺得「�>
  Object.defineProperty(document, 'domain', {
    value: '35.2t? First of all, in terms of courses, I hope to put good and bado the server, and there may be some sensitive data in between. T��取裡面去抓，不用再跟 Server 拿資料了。

# Expb fa f9 04       CallUndefinedReceiver1 r1, r2, [4]
  277 S> 0x2cache probing
    const now = performance.now()
    await fetch(ipt 中的參數傳遞：call by value 還是 reference？'
date在畫面上，所以每一次都必須透過 comment 這個參�title: Intigriti’s 0521 XSS 挑戰解法：限定字元組合�mented a flawed check, can you spot the bug?

You can bypassowser and using AJAX to retrieve data are two completely diff個很大的 message 的時候，花費的時間會不一樣。
eturns the function that invoked the specified function. It retu](/img/cors/story/02-cors-error.png)

小明沒有看得很懂�
1. Simple syntax and easy to get started
2. Easy to customize
3s, it's because you didn't consider these edge cases.

But even 傳的東西是一個 action 就好，那個 action 就會被送�問題完全是個美麗的錯誤，我本來要寫的主題是攻擊。

不過這邊要提醒大家一件事，如果你想�gs need to be considered:

1. Be aware of where users can enter 的 server，誰知道你的 server log 會不會記下來，然�後，也會知道更多的解決方法，就會對這些運作�

## 理解非同步的最後一塊拼圖：Event loop

前面� 跟 HTMLCollection 結合在一起，就能夠達成三層：

 INFO: Access modifiers changed from: protected */
    @Override�瞭解 SPA 所帶來的優缺點，你在選擇要不要用的� appended after numbers to bypass validation.

Since the `boundareserved and will not be replaced by other uses.

The main purpo.

Standards will continue to evolve, and new standards will app�實一開始讓我最困惑的點是因為我英文不好，不 e.target.value;
    setQuery(value);
    fetchData(value);
  };what this story represents.

The protagonist of the story is a s沒找到什麼然後對這個主題也不是這麼熟悉，就�6%E9%9B%86%E7%9A%84%E9%82%A3%E4%BA%9B%E4%BA%8B%E5%84%BF.html)，�頁面的同時，一樣發送一個刪除的 request 出去，�CSRF认识](http://m.2cto.com/article/201505/400902.html)
4. [

1. CSS 越來越多以後很難維護，只要不小心一個�ion constructor 來建立函式：

``` js
var f = new Functi。

但假如使用者是過 60 秒之後才重新整理，�x87410/demo/raw/master/android/demoapp.apk

跑起來以後長�正看不懂的你就寫起來就對了。

到這邊我們已�

``` java
.line 35
invoke-virtual {v1}, Lcom/scottyab/rootbeer/cessary to prove that 418 is actually harmful.

### Supporting rize yourself with algorithms, and it's also a great method, but ghly recommend that you take some time to scan the spec, at leas��的辦法，節省很多流量。

因此我們的目的是�沒有效率。在這個版本改成程式執行時就直接從�
It's important that DOMPurify allows any attributes that start �東西很多都不用學，畢竟現在有些工作的門檻低�別在哪裡

1. 知道如何使用 Flexbox 來排版

1. 知�cation. Just like when you want to study type conversion probleror message.

## Other payloads

There are other payloads mentiomain)` 指令，那有可能執行任意的 JavaScript 程式牀以只要有一個字符合，就是符合這個規則。

冸�拿到回傳值為止。

如果後續的指令本來就都要ommand->getField('ameliaNonce'), 'ajax-nonce') &&
        (
     same thing again and again. The implementation is a bit trivial to construct executable code without those characters.

## Star)，看來搞不好還不少 web framework 有這個 parsing 行       JumpIfFalse [4] (0x2725c3d70de3 @ 57)
         0x2725c3d7rget){
  for(var i=0; i<array.length; i++){
    if(array[i]==tobjects, so it is impossible to generate `dangerouslySetInnerHTMdUpdate 的第三個參數
    if (prevProps.list.length < this.呢？你可能會覺得我在講什麼東西，怎麼會有這� probably wouldn't have written this article.

Several points thxecute arbitrary code? For example, if I want to use fetch to PO�？

只要找到一個夠短的 payload，看起來就沒什�要再進一步反編譯之後才能看到裡面的內容。

�
  ReactDOM.render(
    <AutoHideSnackbar
      onClose={() => {   if (window.TEST_MODE) {
      // load test script
      var s�：[Matters News](https://matters.news/)。

這是他們的登  children: [
    {
      tagName: 'button',
      children: 'senctions, and Performance](https://cdb.reacttraining.com/reactequest.cookies.get("FLAG", default="uiuctf{FAKEFLAG}").encode() �可以拿到 response。

更多細節可以參考：
1. https:erate all methods on the prototype first, then load the library,g this syntax may choose, for the sake of readability, to specif�：

![detail](/img/origin-trial/p2-detail.png)

上面會簡�cks/connection-pool/">connection-pool timing leak</a>, we have tommandResult);

        switch ($commandResult->getResult()) {
 I haven't reproduced this problem myself yet, and the solutions ted as code to execute, so as long as we replace the payload wit->

## Web - newdiary (14 solves)

The challenge is a typical nomo 出來：

```
寫一個 nodejs websocket server，然後用�name)
  // 還可以印出特定 class 底下所有的方法
  id, 'fetching...', followers.length)
    if (data.followers.lengt(4 solves)

第一步是用 tera 的 SSTI leak 出環境變數＂

第二個缺點是一個檔案只能有一個 component，我著上一篇講的重新打包，安裝在手機上，打開 appiti's XSS challenge many times before, so I won't go into detail呢？因為頁面在第一次載入的時候，從 Server 回�bio, posts) {
  
  // if bio or name is japanese, must be japane��結

在寫爬蟲的過程中也是踩了滿多坑的，其�by sharing」

不過，這一切只是我天真的想法而已�yle>
  .profile-card {
    padding: 20px;
    margin: 20px;
    ：[利用 Github Classroom 加 Travis CI 打造改作業系統]er the URL is valid. If it is valid, it continues to check 都是在講同一個東西。

仔細想想就會發現，這�findViewById(R.id.textview_first);
    if (new RootBeer(view2.geex-direction: column;\n  align-items: center;\n  justify-content.

And React also has some checks when setting attributes: https底下是 xml 的內容：

``` xml
<?xml version="1.0" encoding��新的，前者是 Next.js 14（剛用的時候是 13.4，第�e.

Speaking of the issue of compilation and interpretation, I hdf4 @   74 : 81 02 06 02       CreateClosure [2], [6], #2
             LdaUndefined 
  295 S> 0x2725c3d70e19 @  111 : a9       �碼來寫的話該怎麼寫呢？或許可以這樣寫：

``一個是嚴重性（severity），另一個是可能性（possi�卻從來都不理解 position: absolute 到底是根據誰定�nking, I suddenly thought of combining these two words to become` is used, it will start searching from `lastIndex`. If it cannons)，就算禁用 HTTP 快取也依然開啟 bfcache，導致�就回傳新的，沒有的話就回傳 Status code 304，代表�哪些提案可以過關之類的，而最後那些提案就會�有另一個限制要繞過。

## Rate limiting

增加暴力� searchParams.get('redirect')
```

這樣會有什麼問題呢�ction add(a, b) {
  console.log(a+b)
}

add(1, 2)
add.call(null,//www.cnblogs.com/ider/archive/2012/04/01/binary_search.html)
2fore, it doesn't actually generate the same Symbol, it just help
## 實際案例重現

這次要來 demo 的案例是 Snackbar om Sentry's patching process, and reviewing the browser's source�二點，什麼又是 activity 呢？

你可以把 activity �ig` is created from `Object.create(null)`, we can pollute `Arrayta)
  }
});
```

可以想成就是直接在第二個參數的�已，我可以去滑個 Instagram 再回來就好。

可是瀏的介紹，希望對大家有幫助，有什麼疑問或是寫�em.id} item={item} style={itemStyle} />)}
      </table>
    )
 

（附註：這個標題其實是一個 [YouTube 頻道](httpstps://discord.com/channels/656258740252704788/97831218915903080 to know that not only browsers, but also various crawlers will  final node has many possibilities. The reason why the title is ess of path |

## The magical document.domain

After discussing   let userData = { isGuest: true };
    if (req.signedCookies.us423209996C2E84A18EA8446685DAF9FAD4B1D6D8DF0F378EC27D9A81AD4D1A2B`` js
function createPaymentRequest () {
  var methodData = [{�擊者就可以傳入：`https://example.com?redirect=http://12s supported, all others depend on keyboard layout or browser
 * cript code and pass it to eval for execution.

To understand the��

先引一下 Google 的說明：

> Same-site cookies (nd a more secure way to authenticate users. No cookies, no probleoncept should be similar). In short, it is the test data that ich](twitch.tv)

```
#EXTM3U
#EXT-X-VERSION:3
#EXT-X-TARGETD<html>
<body>
  <iframe name="config" srcdoc='
    <a id="apiUrllugin-react can't detect preamble. Something is wrong. See httpstion)` 已經很短了，但以這題來說，還有一個更短ange={handleChange} />
      <button onClick={handleClick}>teslapen-source projects on GitHub.

When displaying a message, it carset first to reduce the time and request significantly:

```  suspicious path.

This page actually imports a file called `htt�該行不通了，後來我想說：「那既然會出錯，有onst handleClick = () => {
    setOpen(true)
  }

  const handle��就會知道，就可以針對這個事件做出反應，例�tr) // 要讓這邊輸出的變成 world
}

// 只能改動這�va.

In this press release, we can also see many features of Jav-

## Preface

Three years ago, I wrote an article: [Easy Unders:

![](/img/intigriti-1021/p6.png)

I actually tried it out, and increasingly popular and eventually became the mainstream appro{
    token = req.cookies["token"]
    if (token) {
   ，比較好拿
2. 某些非同步操作流程比較複雜，透       {data.hits.map((item) => (
          <li key={item.objectta = base64_decode($parts[1]);
  }
}
$sign_fname = $row['id'].'_'].split(',')[0] === 'ja') langPath = 'ja/'
      res.redirect(o� selector 的理解。

再來就是跟排版相關的了，要��提示：「Hint 1: non-standard css properties might help you'input', (e) => {
  searchWikipedia(e.target.value).then((data) cookie, think about what would happen if it could be written tonly be accessed within the internal network via the bot, so th/docs/design-principles.html#scheduling)
8. [浅谈React 16中皀以叫做「入門」，就是因為完全沒有提到這些實呼叫，可以保證你拿到的東西一定是最新的。

```` js
async function getUserInfo(uid) {
  const url = `https練習。對我來說，最大的不同點是後面那個用陣�因的判斷，不一定是正確的。這是我當初剛碰到裡面可以把字串當作程式碼執行的有：

1. eval
2. �是這篇：[Exploiting XSS with 20 characters limitation](s.render("admin", { error: "Incorrect username or password!" });n, or their schemes, hosts, and ports are all the same to be theoudfront! This way, every response will definitely have `Access-因為那時候剛好有一個需求，想要把自己的文�ercel/next.js/discussions/46722)。

順帶一提，我目前完
  // 讓 obj 變成一個新的 object
  obj = {
    number: ob改變了」也沒有到那麼精確，因為在 didMount 的時      "input":{
         "email":"user@example.com",
         "t�概念，那「非同步（Asynchronous）」當仁不讓，絕
```

So how do we actually call these routes? In `src/I 的 method 會直接回傳結果（也是因為這樣所以才�on.href` 會觸發 cross-origin 的錯誤。

![p1](/img/dicectf錯誤的話也會跳出 `alert`：

``` jsx
import React from "不過這時新手可能會有個疑問：「剛剛文件上明�.com/maple3142/My-CTF-Challenges/tree/master/HITCON%20CTF%202024 powerful argument in my opinion.

Everyone knows that 418 is antial issues could arise during this installation process?

Firstthis package, the system will execute `pwn.sh`, bypassing the orux-form 的原始碼，研究了幾個小時終於找到出問�eg1](/img/regexp/reg1.png)


截圖的這個網站叫做 [RegEx1
先來講講我以前的答案，我之前是用下面的定�'` header，讓 proxy server 那邊把 header 輸出，此時對e-of-stealing-data-using-css-and-scroll-to-text-fragment-featureething similar, called `PingLoader::SendLinkAuditPing`. There is that do not want to be treated as the same website." I'll give`//`, so it can be ignored.

However, if you actually run the ab�接著可以來看一下實際在前端呼叫後端 API 的程�但是 virtual DOM diff 也是需要執行時間的。雖然�uests that are simply too punishing for page load time in produc> The scope chain is initialised to contain the activation objection.

In some programming languages, named parameters are suppotw/2019/11/26/dont-break-web-smooshgate-and-keygen/), in which I *a = temp;
}
  
int main(){
  int x = 10;
  int y = 20;
  // 卼�讓網站有更多時間可以更新。因此，origin trials 15]}`` `
```

把上面這整串貼到網頁上的 input 然後�r 動態建立函式

這些知識在什麼時候會有用呢？, when converted to a string, it will call `join`, resulting in ave another place to worry about. If this URL gets hacked and thge the encoding
      of the document and cause it to be intern find some things we need, such as `eval`:

``` js
let _onmessapoint, the focus is on CSP, Content Security Policy.

CSP is a s    // console.log(err)
      }
    }
  )
}
```

把這幾個核子找到的一個洞，用的是最基本而且經典的攻擊�direct 的話，網站可能就不會跳出提示（因為是同 http.StatusBadRequest, "url parameter is missing or is an invalt intuitive ideas I have is to add a single quote in front, so tlities, but if you look closely, you'll find that they are specid have heard of the term module. This is a simple example.

For  來決定要不要改變 state，是很常見的應用場景：取出來，或者是用 `[...result]` 把它轉為一個陣列�真的很多點可以吐槽，但現在已經有點忘了，我�就跟我要證件核對身份，核對身份完以後就會絋是有些瀏覽器可能不會帶 referer，第二個是有些�g）。測試寫得好，程式就不怕改壞掉。因為只要   filepath = os.path.join(app.config['UPLOAD_FOLDER'], f.filena有最後一個問題要解決，那就是剛才提到的 reques所以無論你從哪邊來，都會是登入狀態。但攻擊�sm: false,
    wrapper: 'none',
    strict: true
});

app.post("�碼的。

我在網路上找到一篇跟我論點一樣的文f5.png)

> o 這個變數的值是什麼？

這個問題的答�

根據我們上面所得到的知識，可以插入一個 id �      mode: 'no-cors'
    });
}

const block_socket = async (i) d error handling. When the return value is false, switch to a rey points:

1. Commas can chain multiple expressions, returning tiption is written like this:

> the date-time notation as defineot 的 index 為 4 的位置。再來我們回顧一下前面所�� [Reactive Programming 簡介與教學(以 RxJS 為例)](http:ameter count 1
Frame size 0
   88 E> 0x3e0ed5f6b022 @    0 : 91  it works.

After understanding how it works, I began to wonder �比較踏實

這篇就簡單記錄一些心得跟資料，� 還繼承上層的執行環境。

因此你無法存取到 topought/)
2. [淺談 XSS 攻擊與防禦的各個環節](https� GraphQL 去抓結果：

``` js
memo(id) {
    const query = `://www.sjoerdlangkemper.nl/2016/04/14/preventing-csrf-with-sames�關的內容就到這裡了，幫大家複習幾個關鍵字：5555" from accessing a cross-origin frame.

Because although alid enough. After all, I've been writing CSS for so many years, an�也不多。

這邊稍微提一下一些基本的但我沒講帊也超級方便，你就設定要用什麼 HTTP method，他�he `timeout` will be set and can't be change. If we can let the , starting with Chromium. I will use the latest stable version 1 only provide a warning, while the paid version can be set to blked by default.

Let me demonstrate this for you. If you execut�，節省超級多時間。

在看其他人的專案時，我�10);
  if (!isNaN(num)) {
    total += num * 2;
  }
}
```
/2021/07/10/cookie-bomb/) 來做事，但討論過後我們也�  if (params)
      for (const [key, value] of Object.entries(paame Origin?
2. What is Site? What makes it Same Site?
3. What isabove, I thought I was done, and I didn't expect there to be oth.

In JavaScript, you can dynamically create a function using `n？

``` jsx
const ReCAPTCHA = ({ onChange }) => {
  const divRe�最下面那一條，看圖解應該還滿好懂的，就像是�值也是同一組 token。

``` html
Set-Cookie: csrftoken=熟悉的流程，因為一大堆網頁都是這個樣子做的�aptcha/`, how to overcome this?

There is a subtle diffe data for each page. After fetching the data, it calls `renderTo��你有一段程式碼，有一個按鈕以及一段 script，�ig">
    <input name="enviroment" value="test" />
  </form>
  <s�哪些學生沒有 po 的表單，我只能一個一個自己�status/{id:[0-9]+}', UpdateProviderStatusController::class);
   v1 比較快」

答案是兩個一模一樣，因為如果你夌第一點是 error first 只是個「慣例」，實際上會�要做的是先看看路線圖，知道有哪些東西可以學�有的函式都是閉包：談 JS 中的作用域與 Closure](h8
tags: [Front-end,JavaScript]
categories:
  - JavaScript
---

#SA examination also includes an intermediate level of web applicml
<div>
  <form></form>
  <form name="scripts">
    <img name="'t exist, what does that mean? It means that it has sent out theer, since the structure of this i18n is always the same when usetOpen(false);
    }, duration);

    const timer2 = setT2. 為什麼要有 SPA（Single Page Application）？

<!-- m��很推薦大家去聽聽看：[140: Evan You - Reimagining the/+/refs/tags/100.0.4875.3/third_party/blink/renderer/core/css/csey are not completely confirmed yet, but if there is one that ylayed in Taiwan time, not the user's computer time zone.

At thiseconds, meaning that as soon as the browser receives it, it wi
isset($_GET['source']) && die(highlight_file(__FILE__));

funct��的 exploit 長這樣：

```py
import requests

#0xAlessandro }
  }
}
```

The result will look like this:

![](here should be no way to escape the attribute, and `>` has also bg Part 1: Decompiling and Rebuilding APKs](/2023/04/27/en/androi者清單裡面出現一大堆日本人。

因為他們有些�個結合，而中間的 id=a 順利變成了屬性的一部分�proach to take. So the purpose of the range is to define the pe.com/). GitHub's CSP is set very strictly, and almost every typ1
    new-instance v0, Ljava/util/ArrayList;

    invoke-direct at kind of problems? How to trigger it?

``` js
var tmpl = '<inpample, suppose for some reason the testing environment cannot fu是範例，所以就省略上述步驟，並且直接把上一�則不會。

但問題是這邊我們沒辦法控制 path 的�組合，在 functional CSS 裡面每一個 class name 都只負="en">
<head>
  <meta charset="utf-8">
  <meta name="robots" con差異
2. shouldComponentUpdate 的作用
3. React 的渲染機�資料時，就已經是 render 好的結果了。雖然�函式簡化過後的版本
getAPIResponse((err, response) => {
ub.com/Lidemy/mentor-program-3rd)改編而來，而且是秉持�
my_balance -= 999 // Uncaught ReferenceError: my_balance is not
```

We can use `$'` which is `">` to close the tag, thenl string to be executed in the hash and dynamically executing thCORS would never be a problem for me again, and I would never se及如何修改 smali 程式碼。

系列文連結：

1. [Andr��，儘管高手還是可以做逆向工程，寫出一個搶�月用同樣的手法繞過[微軟](https://thezerohack.com/how-r 觸發，把 open 設成 false，再度觸發 transition，有�以不會再次觸發 render，就看不到對應的新的 statCredentials', true) // 新增這個
  res.json({
    success: trrective": "script-src-elem",
    "effective-directive": "script-uess what it became in the end?

All the tags inside the select s not as simple as imagined to realize which parts are user-inpunt 看個前幾行就知道了。而 Vue 的話則是 setup 的倂

而改成這樣以後，思考的點變成：「當 state 攅�惱，想要做 side project 可是卻不會後端，資料不chema

```js
var database = new Sequelize('database', 'root',s/issues/3157), but no one has responded yet.

I also submitted the United States or in Taiwan.
2. The time on the website will tion seems not working, but somehow the string concatenation alwput is read from a file, it will always be a string, and the fo�可以拿得到），在送出 request 以前，會先使用 Ja�是日本的考試，但是題目中其實會出現三種不同��成果。

## 總結

在這篇裡面我們學到了如何閱�place('{{value}}', value)
document.body.innerHTML = tmpl
```

ealing with time, most people should choose a library that looks�，實作起來比想像中簡單許多。

除了這個以外�去偵測 response time
3. 如果 response time 小於 threshold�即可。有變動的地方是前端，原本你在前端需要�似乎也比較少一點。

話說最近我發現自己的 wrigle.com` 開頭才可以。因此我們需要找到第二個 operor.prepareStackTrace = oldStackTrace
  }
}
function trigger() { series of levels that you have to pass one by one. Every time I/es.png)

The most interesting part is the year, which, in additwe want to discuss in this article.

So how many types are theresandbox="allow-same-origin" src="/memo?memo=${xssPayload}">`
con call API 拿資料」，而不是「當使用者按下按鈕時��這件事麻煩的點在於你必須直接去改 node_modules マネジメント試験在考什麼？

我自己覺得考試兜想用 function to string 的方式拿到 v，Chrome 跟 Firefo      text = text.replace(/\*\*([^\n]+)\*\*/g, (match, p1) => {.png)

Moreover, this limitation is not just for a single requehing after `/*` is also a comment, so this entire section is actarJS is scoped in a local object, and you cannot directly accessisQuarantined":false,
   "type":"User"
}
```

除了基本的自redux 的應用裡面，所有的 action 都會通過 middleware��換率。

而上面的三個步驟，最重要的就是第�rg/p/chromium/issues/detail?id=165452)。

## 結語

以 sessioement('script')
            el.src = 'http://test.ngrok.io/block測驗，最簡單通過率最高，但是沒什麼用，不能手機上面。

而接下來的這一篇，我們要來看看如�名，而 web 題也只剩一題沒解出來。這次我基本�list: [...this.state.list, 1234567] // 增加一個元素
    }).ptt.cc/bbs/C_and_CPP/M.1245595402.A.2A1.html)
5. [ECMA-262-3 inf (typeof obj[key] === 'function') {
    console.log(key)
  }
}
可以知道樣式長怎樣，你不必在 HTML 與 CSS 之間�而且這個名稱 `helloB` 跟你想的不一樣，他是沒�anged value of a variable before the value actually changes](httnce this request was sent by `<script>`, it did not include the raries provided. However, in the JavaScript ecosystem, therehar code to do the binary search. Also, I assumed that we know t }
    userIdMap[userId] = true
    utils.log('userId:', userId)漏洞最近的已經是 2017 年的事情了，而且是使用丳號密碼登入，就開始測驗了。

會先有一個三分t opened the chat
        first_sid = create_sid()
        js_pa�我知道我會很多，但我也知道我不會的很多，每split('').map(c => mapping[c] || 'errorerror:' + c).join('+')
}
e registry to reflect that.

When I was researching this whole te support of the browser is not so high, especially when importifficult, are quite troublesome to implement.

In addition, it catart')
      inp.value = 'a'.repeat(10000)
      f.submit()
    typeof title !== "string" || typeof text !== "string") {
  �按鈕，點下去之後就可以把一篇文章刪掉。當初//blog.bawolff.net/2021/10/write-up-pbctf-2021-vault.html

作耀下，發現居然還是有錯誤：

> Access to fetch at 'httallback) {
  var cache = this.cache;
  var done = callback;
  vafptr<const SecurityOrigin> ping_origin =
      SecurityOrigin::Cpt code. Each of these has different attack and defense methothe disk cache.

![disk cache](/img/http-cache/p1-disk.png)

How. 當 grecaptcha 的 callback 觸發時，會呼叫到的是 han��是先`map`之後再自動幫你壓平。

所以，我�下`takeUntil`的示意圖（取自：http://rxmarbles.com/#tak�假設我們實作一個非常簡單的，按一個按鈕之後��名稱。

React 對於這些按鍵相關事件的處理就到uest doesn't pass access control check: The value of the 'Accessferent page, causing a "TypeError: Failed to
// fetch" error andence I saw somewhere before: "Instead of asking a question, ther perfectly supports Markdown syntax.
2. Uploading images is sup� opacity 會是 1，而根據官方文件所說的，使用者�簽證）

其中有一個條件是必須要證明你有從事輚

```js
fastify.post("/create", (req, reply) => {
    const {��？

## Tagged templates

有用過 React 中的 styled componn the future. After solving this CORS-related problem, I think I��並不支援深色模式，因此要自己做一個。

我改�我們這樣寫，用`mapTo`把每一個`mousedown`的事件�
    <p>LINECTF{rotate-1 yellow-bg"}</p>
  </div>
</li>
```

其x87410/blog/issues/36)。無獨有偶地，這幾天也看到�架，後者是網頁的衣服。HTML 就只是用特定格式�到最後，會發現其實都只是這些操作的組合而已�M Library，只要先定義好一份`schema`，就可以幫你�face that web developers can use to schedule asynchronous and noes, you read that right, it only reduced by 1KB.

When I saw thi21/06/07/xss-challenge-by-intigriti-writeup-may/)
3. [Intigriti t enough to defend the first level, and we must also defend the r，顯示在頁面上面。

就這樣簡單幾行，就完�，你要寫壞其實滿難的。

而這個特性的差異，乥文裡面就很常用漢字，就算不懂日文，也能靠漢��，存檔以後看個畫面再調整一下就行了，跟以剚

1. [[stagecafrstore.starbucks.com] CRLF Injection, XSS](httpsrics.io'
function validateDomain(url) {
  const host = new ction' && typeof item !== 'object');
  if (properties === undefi�以必須先有 Execution Context 以及 Variable Object 的觀 it called Lidemy?

Previously, someone wrote to me asking ford in an iframe:

``` js
name = 'Console'
document.title = name;
second one is about "SECOND".

When I saw the word "SECOND", I r是最好的」或是「為什麼不乾脆怎樣怎樣」，但�的指標，不是也可以達成嗎？可是 C 又說只有 ca
      R = M - 1;
    } else {
      L = M + 1;
    }
  }
t a valid custom element name
  ["foo-bar", ""], // valid custom  this.endpoint = host + "/";
        this.endpoint += option.pasearch.securitum.com/xss-in-amp4email-dom-clobbering/)，底。

npm 全名為 Node Package Manager，這邊的 package 就�那些元素原本不存在 index.html 裡面，是我們後來�省了許多開銷。

# shallowEqual 與 Immutable data struct mock(str);
}

WebAssembly.instantiateStreaming(fetch('/blazingfnt] = useState(0);

  return (
    <div>
      <p>You clicked {c   data: o(t = t || window.location.toString(), r),
    自訂 header

還記得我們一開始串的那一個 API 嗎�>
```

What if the players never heard about `SmoothGate`? They ddenly broke through the blind spot and thought that I didn't ne plugins to do some conversion. It sounds like a very promising n that takes a positive integer n (1<=n<=100000) and returns trut is correct or not? In the field of programming, this is act如同它自己在官網上所說的：

> Node.js® is a JavaScframe.contentWindow.postMessage({
            eval: `fetch('${cao d? Have you seen it?
Execution result: No, so it returns an ertall` and other scripts are disabled by default (this was availa these are just some terms. I just want to supplement the text i大之處就在於它不僅限於此。Vite 在 GitHub 上的 Abture Discovery
1. Cross Site Scripting Attacks
1. SQL Injection
��生錯誤時留下一筆 log，也有可能在發生非預期� can add our own content and prevent it from being rendered by t).catch(err => console.log('err', err.message))
}

function merg origin-domain? Let's take a look at how the spec says:

> 1. If removing them will save space, so it's better to remove them.

eself, but I never thought I would see a real-world case of a coersion was released.

It seems that news about supply chain attaver, when doing string replacement, there is something called [sellipsis;
  display: block;
  display: -webkit-box;
  -webkit-liggy, and I have no plan to maintain it, so I will shutdown the slues, we should investigate them thoroughly. From the screenshotations.

This announcement even mentioned the behavior reported )
```

Simply put, it loads the configuration file and then runs� Fiber 底層的運作（因為我也還沒認真研究過）�ity Operation Center） 跟 IR（Incident Response）等等。

-result-object v0

    check-cast v0, Landroid/widget/TextView;
don't want to bring cookies together, you can add `crossorigin`./* loaded from: classes.dex */
public class FirstFragment extendub.com/v8/v8/blob/d84e9496d23cf1dc776ae32199d81accfabaafb5/src/i�

``` css
@font-face {
  font-family:"A";
  src: local(Arial);
1] + arr[i][2] 
        let prefix = arr[j][0] + arr[j][1]

    payload:

```
not@not_exist.ext" || (this.password.startsWith("Iconst found = await testChar(flag + c)
        if (found) {
  If you achieve these three points, you have succeeded.

I strong` 改成 `\\d`，不然的話會被當作跳脫字元來看待�.log(level1.level2.level3.level4)
    }, 500)
  </script>
</boditle: 透過 Chrome Origin Trials 搶先試用新功能
cataloon for doing this.

These issues can also be referred to in [Frohis is incorrect.

Suppose the restaurant we are visiting is in  一樣嗎？有什麼特別的？

有，還真的比較特刌可是現在我們前端已經變成一個靜態檔案了，�來解析並且渲染。

而作者發現了另一個 bug，就://hackmd.io/fmdfFQ2iS6yoVpbR3KCiqQ#miscundefined)，這題有� the plugin... So I turned to study the package that appeared inusses this issue.

In short, I prefer to use the phrase "programshould also not cause any problems.

Dealing with time is reallw not success. After having the ability to store the flag, we caxp

Continuing with the small challenges at the beginning:

```  done at build time.

Therefore, one approach is to render the p�我們只要記得一個概念：

> action in, action out
��元素換掉，而答案就是 A，根據最後參考到的時/ "function String() { [native code] }"
```

而且一旦我們�tent!');
    return <div>Content</div>
  }
}
  
class App extendto the instance and skip the fetch
    context.instance = instaxample code above step by step:

``` js
var foo = "bar"
var a =not change the last element, in fact, we can wrap the entire parpported `mathjs` later to try it out.

Let's try it out. First, `"123123123"`, and all of this is thanks to the extensibility of��能以為沒有，我以前也以為沒有，我同事也這�不到，就會再往上一層直到找到為止，所以你可䔨域，那最後 log 出來的值就會是 200 而不是 100；ub.com/justinsteven/advisories/blob/main/2022_git_buried_bare_rewithin this iframe, there will be another sandboxed iframe conta�候，不發送 request，只回傳一個空陣列，而回傳�title: 網站前端打 API 時把密碼加密，有意義嗎？
�狀會被蓋掉的方法可以用 `with(a=source,/b/)` 繞掉�js.org/en/docs/guides/blocking-vs-non-blocking/)也是把這兩�bj.toString() === '[object Object]'
}

var obj = {
  toString: f��件的相依性，然後根據這些資訊把東西包在一�eturn ((n * 100 + 20 - 4)) % 10 + 3  // 計算價格公式
}
  
��實作的原理來看，那 call by reference 不也是 call b搞清楚透過參數操作物件時，會有什麼樣的行為�  if (n <= 1) return n
  return fib(n-1) + fib(n-2)
}, 10)
```

s
[]['constructor']['constructor']`_${'eval(location.hash.slice(docs/reconciliation.html#motivation)
[reactjs- Optimizing Perforludes a clearer table, which you can take a closer look at:

![rcument.head.parentNode.remove(); // hide code if not on iframe
 r);

// 改過的，加上 decodeEntities 處理中文
// https:字去操作。就像我上面的截圖那樣，打一個神奇�ks can extend Node easily enough if they want to play with non-suld become 00 in 2000.

These historical events are mentioned inhought about whether to write one myself, but when I thought ab  if (timer) {
    clearTimeout(timer);
  }
  timer = setTim page.type("textarea[placeholder='Paste contents']", "Flag");
  了…*

版本控制是一件難事，尤其是透過這種傳缌你看到好 code 的時候怎麼知道它好在哪裡？你丝�有大量的註解，所以看著註解就可以理解五六�

What should we do now?

In `log`, there is a line that says `dhat if you insert a script tag using innerHTML, it won't execulities in the CSP rules, you can go to [CSP Evaluator](httpx-height: 300px; max-width: 300px; display:block; margin: auto; pport for another one called Prism.js, so I switched to it. I ju能是透過 2023 年某次更新植入的供應鏈攻擊，或�0e9de176b6db28512f2e000b9d04fba5133e8b1c6e8df59db3a8ab9d60be4b97return "[object Array]" === Object.prototype.toString.call(t)
  (n-2)
}

console.log(calculateFib) // ???
console.log(fib(6))
``efore, we can trace down to `requestBeaconResource`. Addied to try to save 418.

Recently, I spent some time studying thmuch.

Anyway, we used this trick on our website and inline CSS  beginner's tutorial", it talks more about modularization and thion to finding a method that can return `this`, I also want C9ZvgZ8Ja5Xgg02nxgAABydOirGLoL4htFsYVtwLrNg8NeSEanLwYLCVaSqkjANR後再裝回去，這篇就來記錄一下流程跟心得。

<!a-262/10.0/index.html#sec-assignment-operators-runtime-semantics//",
  "NODE_OPTIONS": "--require=/proc/self/environ"
}

requireextensively to enhance the difficulty of static analysis. We ca�前面的話，我就只要寫：`function cb(err, fileName, fi�我們必須找出每一個做檢查的地方，然後都做類��嘗試了大概三四個小時以後，發現行不通。

理��了一大堆東西：

``` python
import traceback
from selenid the page in the iframe will not have a domain attribute, so tht the packaging was successful.

One of the benefits of using wemethods, and found that this is indeed the case. The GET requesitions, or the user manually removed the alt attribute.

So thexbus@6.4.1`
10. `asciidoctor.js@1.5.9`
11. `aurelia-script@1.5.2�講先不談的「成本」。

若是真的比較安全，為�sponse 回來之前會不斷印出 waiting，直到接收到 res'AWS Lambda + GitHub API + Google Sheet = 自動化簽到系統'�市、大安區、大安森林公園，在大安森林公園運fined` because the length of the array is 1.

Here comes a cool f downloading malicious packages (of course, this doesn't solve e are most concerned about, CORS or other related operations. It'; require-trusted-types-for 'script'; trusted-types default"
  體大小或是檔案沒有權限開啟等等，都會透過這�突然想到了一個可能性，那就是 Chrome 沒有照著 s have an opener by default](https://github.com/whatwg/html/issuequests
import json
import concurrent.futures
import string

BASEe file name to `mjs`. For details, please refer to: [Node.js v13 been downloaded. The browser interprets it again (hence Univers`csp` on iframes, and by adding it, we can set the CSP.

As ment      <h1>Snackbar</h1>
      <p>靜態方式顯示 snackbar</p>�說，就會輸出「讀檔失敗」這四個字。

可是如�tps://play.google.com/store/apps/details?id=com.kevinzhow.kanaor可以繞過，在公佈答案之前，先來看下一個段落�log(res))
```

打開了瀏覽器，發現可以成功拿到選�ument('--js-flags=--noexpose_wasm,--jitless')
    options.add_arsly htmledit.php, which allows us to insert some HTML.

## DOM cjects/String/replace#specifying_a_string_as_a_parameter). For eumbers have a range? Yes, they do, and the upper limit is `Numbed have a basic understanding of each type. Before we start, you sage')
  }finally{
    process.exit();
  }
});

// index
module.�對後面的 domain 並且記起來：`/^.+@(.+)/`  
最後再䃅！

我並沒有把實驗的變因處理好，我一直以為://www.jianshu.com/p/9931a1e77066)
2. [APK反编译之一：基�e.log(123)')
f() // 123
```

這邊 test 可以是任意函式� 秒左右，預設的 timeout 好像是 3 秒左右，記得要(/img/what-is-log4j-and-log4shell/command.png)當攝影機看到 it took some effort, this is just the beginning.

## Level 2: Mtheoretically, but need to wait much longer.

## A faster soluti8080
```

Then you can open it: http://localhost:8080.

Unfortu��手機號碼的 RE 寫出來了沒錯，但你沒有覺得很�到 hash 的密碼還是能保證一定的安全性，可是在�let browserOpen = false;

const visit = async code => {
  browse就算你後端壞掉，你前端還是看得到畫面（只是�之前一樣，讓學生在某個 channel 裡面每天自己 po 個分頁，沒有設置 noopener 跟 noreferrer，所以新開�('insertHTML',false,'<img/src=x+onerror=alert(this.parentNode.ine are some small details that need to be paid special attent## 跟 inline style 差在哪？

1. Inline style 不能有 medi��個方式是直接去改變 okhttp 的實作，找到 smali �.

As for Firefox, from the status of this issue: [meta] Enable nguages is also similar. However, some may only be accurate to etail here: [Gaining security and privacy by partitioning the cad CDNs to circumvent CSP protections - SECT CTF Web 400 writeup] 如果使用者產生了新的重設密碼 token，舊的應該o = o.substr(0, o.length - 1), !n.test(o) && c(u) && (u = i(r, e��候有了改變，因為 ES6 的規範裡終於有 module 了�ng the `document.domain` modification feature, it needs to incluervice worker.

It's really strange. I tried testing it manually still many problems to solve, such as:

1. Dependency, for exmporting third-party modules," and does not manage anything elseirectly.

The next step is how to make `onerror` happen faster taccountName":"test@test.com","password":"1234"}`

#### Netflix

�？「網頁前端」，這同時代表著「網頁裡的前端�種，簡單請求跟非簡單請求，簡單請求的話可以blem but also addresses the user experience. When a user visits urn res.redirect('/notes');
  });

  fastify.get('/notes', auth(og(1)`, `alert(1)`, and finally return true, so the result of thquite a few things, it still has time and versions, and `4.4.3` ��後 `setTimeout` 屬於 Web API，所以會跟瀏覽器說：�「A」這個按鍵的 keyCode 是 65，而「A」這個字元�的範圍）

1.的話幾乎每家公司都會有，但我不�.html)
2. [Re: [Discussion] Why are SQL injection and XSS vulner we also need to get this image in JS, so we use fetch to get itt starts and performs the initial rendering, at which point the 到既然是個停止維護的產品，繼續在那邊就會有�這個值寫在 session 以外，同時也讓 client side 設宋如說這個：[[cs.money] Open Redirect Leads to Account Takeo
  }
]
```

Besides prototype.js, we have 11 other libraries tha API, which exposes most of the networking functionality at a loiginal data type, which is a string. JavaScript's comparison of ws, with the goal of achieving XSS to steal a pre-set flag:

```oCOUxwSBgxvLLT37eaByNTxpfWomxIsH671wuydnmMedWyNIqyaMtxBORuiWUiG4sponse('/event', response => {
  // 判斷活動是否開始並詗證碼，他們應該會煩死吧！

### 加上 CSRF token1)]
const data = props.list[1];
  
const nextProps = {
  ...prop�已，就是那麼簡單。

這就好像很多知名的博物�話只需要知道以下概念：

1. 什麼是 package.json

1��開頭的問題三的解答。

這種用法通常會用在哪defined;let Uint32Array=undefined;let Uint8Array=undefined;
    ��以改善使用者體驗。為了因應這個改變，後端�成了。

在下一篇 [CORS 完全手冊（四）：一起看�985624467810189367
) 的討論，作者以為需要 `.git` 這�yword detected')
  }
  document.body.appendChild(script);
}

fun      args: ['--incognito', '--no-sandbox', '--disable-setuid-s*
 * @throws \InvalidArgumentException
 */
public static functntStarted`的時候一定會是 false。

正確的方法是把�apply 與 call 存在的幾個原因，這都是用一般的 funo write: `display: -ms-flexbox;`. And I looked at the prefixes wf some interesting problems, such as birthdays. Birthdays shouldupport this claim?

Keyword search trends to some extent represtring is a sequence of 16-bit numbers, and these numbers are UTieved.

You can imagine a situation where a website has a featur you click a link and navigate to another page, it's not actu= serialize_user(user_string)
    return user
```

The code beloscripts. The starting point is:

``` js
function run() {
  Java.e.at,Math.seeds.push,Math.hypot.name.at,Math.seeds.push,Math.clz, "relative", "path", "directory", "file", "query", "fragment"],後有些你就算唸了也不知道是什麼的，我會特別踀下也是很不錯的。而且會常常跟 webpack 搞混，�容。

但總之呢，因為後端不再處理這邊的路由 Sheet API 了，我稍微看了一下官方的 API 發現還� API intended(4 solves)

這題就給你一個 API 文件，可� I started a simple HTTP server and wrote a simple HTML to run ests!

Compared to my previous solution which makes 185 requesually quite important:

> This is only an effective defense agaiScript. If the API for fetching data takes two seconds to respon�列有的差不多，像是`map`、`filter`、`flatten`等等
bc336a6534aec74c|1b1a758bb26702bc0ac7cd660da2a72866f2cfdcf3668f2 stack overflow 指的就是 stack 太多東西滿出來了，� {
  state = {
    list: Array(10000).fill(0).map((val, index) =�自己去管 Server，你唯一要做的就是把你的 applicaunt()`, the content will be override by mithril.js


For the fiatter does not conform to the ISO 8601 format, so different resu使用場合有限，但真的是個相當有趣的攻擊方式�：Fundamental Information Technology Engineer Examination，簢�。

因此這個階段就是要在自己的電腦上安裝 No要有物件導向的觀念，不然有一大堆用法你會看�return a+b
}
```

所以第一個參數給空字串是行不通�/react/blob/b87aabdfe1b7461e7331abb3601d9e6bb27544bc/packages/reflag{test_flag}"
  }
}
```

靠著這個，就可以順利拿到y answer wouldn't work on Firefox. The reason is this piece of c extremely poor. This one small flaw is enough for me to give upntered situations where DevTools are not sufficient. What shwhat are the advantages and disadvantages of each format. This wrequest)
3. [Introducing the Payment Request API](httpshtml
<style>
  body:has(a[href^="/view/1"]) iframe {
    width:       <tr style={style}>
        <td>{item.id}</td>
      </tr>v1 = 10
function test() {
  var vTest = 20
  function inner() {
 Management Protocols
1. Network Traffic Analysis
1. Networking Issue. Because it is the simplest and most convenient, but the bb tests. It's not clear that
we should send them patches to rem下，我認為我的考試策略是有效的，確實可以在�lement("code", null, "App.jsx"), " and save to test HMR updates.對網站做壓測的時候，發現 preflight request 的數量-Origin` 不能是 `*`，要改成單一的 origin。而後端�(/img/justctf-2022-writeup/p2.png)

我原本的想法是，假�futures.as_completed(futures):
        index = futures[future]
 or `apply` to call it, and the difference is in the way the paracaption: [],
  source: ['src', 'type'],
  iframe: ['src', 'frame�要的。

首先，我們要比較的對象是：

1. 在使�覺得就差不多了。如果上面講的都有好好學的話�事情是：「那有辦法可以不要這樣嗎？」，同事) => s.normalize('NFC');
let u = (s) => unescape(s);
let t = (sypass require at all, you can just use `module.constructor._loadion 誤寫成 function call 以後，會產生的結果就是，��該找得到）。

（話說有些讀者可能有聽過高度偽裝成陣列的物件變成陣列呢？有幾種方法，例�n relying on the date time given by the back-end.

So, how to deame as what I encountered. The solution given in the end is to s thing, which only puts a string and changes it to require thatwser, you must use a tool to package the code first.

And the t, and `<%-` can output unescaped content. So my initial idea wa 找不到就會往上找，一路找到 window。

所以開頭如果今天是按照以前寫 CSS 的方式，有可能他在 C�為你的參數是陣列而不是一個個的數字，直接�{``%2b{}}`[1]%2b`${``[`${``%2b{}}`[5]%2b`${``%2b{}}`[1]%2b`${``[scopeChain: globalEC.VO
}
  
test.[[Scope]] = globalEC.scopeChaisafe attempt to initiate navigation for frame with origin 'httpsed({ extended: false }));// environment config
const port = Numb or text too long");
        }

        const id = crypto.random�得比較難懂或是一直懶得看的一些主題，所以考�到 log 當時的狀態，但是點開物件詳細資料的時� whole flag.

Apparently, we send too much requests in setup phaber to check it again after using it.

## Automatic translation
ctions.

As shown in the figure below, the left is the Node.js rect", "Object"],
  ["ol", "OList"],
  ["optgroup", "OptGroup"],
eSync`來讀取檔案，最後把檔案的內容用`console.log`��用 Google 翻譯來看講解。

底下是我寫完第一輪� corchat x - 1 solve

Author: larry

跟 socket.io 有關的題�8]',
  t: '`${e}`[6]',
  u: '`${e[0]}`[0]',
  ".": '`.`'
}

func828-2c436b80-f6f0-11e8-8c46-3f713e2f37cd.jpg)


The last one isft, which looks like the one above. It's okay if you don't undern the nesting as much as possible.

The above is almost all thl see your code at a glance.

![螢幕快照 2016-03-20 下午4.` js
/**
 * This method scrolls element into view if needed, andlenges that I personally felt I had learned something new from.
�威力。

在繼續往下之前，我們必須先了解一下�I deliberately did not mention, such as other specifications suc3.2.2. HTTP requests
> 
> A CORS request is an HTTP request thatME states:

> Backbone.js is a great tool for organizing your ja`, the injected style is cleared. It seems not work.

How about 

This question uses bfcache: http://web.dev/articles/bfcache

SPRIMITIVES: ['String', 'Number', 'Boolean'],
    loads: json => 0-f6eb-11e8-899a-e1a5652d9e1c.png)

# 總結

Payment RequesdFile('./README.md', readFileFinished);
```

可以看得出來`�る。

至於 B 科目的話比較像是應用題，因為有蠭來這個坑還是落到自己